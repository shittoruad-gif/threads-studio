# 夜間整備への申し送り

朝の点検（`threads-studio-daily-check`）で見つけたが、日中は本番に反映しないため夜へ回したもの。
片づいた項目は消してよい。

---

## ★2026-09-18 11:30 に日中反映した（三上様の「今すぐ出す」のご判断）：お店の情報が「直せる項目・直せない項目」に分かれていた

**きっかけ**：9/18 9:19 森 正一郎様（s.forest89u@gmail.com）からのお問い合わせ。
「アカウントの連携は出来たが、お店の情報を修正しようとすると、修正内容が保存できる部分・できない部分が出来る。
AIがこう考えているような部分は時間の経過と共に修正されるのか？」

**何が起きていたか（コミット済み・pushは夜）**
1. アプリの修正画面（`/ai-counseling?project=…`）の一覧に、**「まず4問」（業種・場所・店名・悩み）しか並んでいなかった**。
   画面の上には「AIはこう理解しました」として**誰に・悩み・方法・未来・なぜ**の5行が出るのに、
   そのもとになる**お客さん像・強み・メニュー・来店後の変化・USPは一覧に無く、直せなかった**。
   公式LINEの「きょうの1問」で答えた分も、アプリからは触れなかった。
2. **古い登録（`counselingResult.rawAnswers` が無いもの）は、列にしか内容が無く、修正画面に出てこなかった**。
   例：`lB1xONFnbe-aHvCUYUd7U`（Moveact玉島店）。
3. 保存のとき、**空にした答えは列に反映されず**、前の内容が残って投稿に使われ続けていた。
   さらに「なし」と答えると、`projects.strength` などに**「なし」の3文字がそのまま入っていた**。

**直したもの**
- `client/src/pages/AICounseling.tsx`：一覧に「まず4問＋要旨のもと＋答えのある項目」を並べ、
  残りは「ほかの項目も見る・直す（残りN項目）」で開く。修正は**番号ではなく質問id**で開く（別の質問が開かない）。
- `server/routers.ts`（`getCounseling`）：列にしか無い内容も答えとして返す。使わない言葉は列と合わせて全部見せる。
- `server/counselingSave.ts`：`askedFields`（画面に出した項目）を受け取り、**出した項目だけ「空にした＝消す」**を反映。
  出していない項目は触らない。「なし」は列に書かず空にする。
- `server/autoPostScheduler.ts`：`runAutoPostCatchUpForUser` を `QA_SAFE_MODE=1` で止める
  （ローカルQAは本番スナップショットを使うので、保存の瞬間に実在のお客様の投稿生成が走りかねなかった）。

**確かめたこと**：`vitest` 916件すべて通過（保存の新テスト4件を追加）・`tsc --noEmit` エラーなし。
ローカル（本番スナップショット・`QA_SAFE_MODE=1`）の**375px実画面**で、たきもと様の登録が20項目すべて「修正」できること、
Moveact玉島店（古い登録）が12項目＋「残り8項目」で出ること、横はみ出しが無いことを確認。保存の通信は途中で止め、DBには書いていない。

**反映の記録**：11:30 に `834297b` を push。自動デプロイ（11:25 起動・約7分）が `finished`。
配信中のファイルが `index-DNkQTC_8.js` → **`index-Czzqh1a6.js`** に変わり、
`assets/AICounseling-lxYKOoEX.js` に今回の文言（「ほかの項目も見る・直す」「空にして保存すれば」「askedFields」）が入っていることを確認。
※本番へのログインはしていない（ローカルの `JWT_SECRET` は本番と別で、`getCounseling` は 401）。
サーバー側の動きはローカル（本番スナップショット）とテスト916件で確認したもの。

**残り**：森様へのご返信は三上様の○のあとで。

---

## ★2026-09-18 未明に済ませたこと

### 1. 慣らし運転のお知らせ → ✅ **仕込みました（送信は 9/19 朝7:40）**
`shared/announcements.ts` に `account_ramp_2026-09-19` として追加。文面は三上様の○が出たものをそのまま。
- **送る相手を絞る仕組みを新しく足した**（`DailyAnnouncement.audience`）。これまでのお知らせは絞りなし＝今までどおり全員。
- 絞り＝**連携から30日以内の方**。**送信日（9/19 07:40）時点の実数は12名**（全員LINE・メール0通）。
  ※01:10時点の数え方では13名だが、三上様は9/19にちょうど連携30日を超えるため対象外になる。名前は
  `scripts/ops/announcements/2026-09-16_ramp_notice_draft.txt` の末尾に記録した。
- 送られるのは朝のまとめ通知の中。1人1回（`lastAnnouncementKey`）。

### 2. 9/17 日中に出た4件の確認 → ✅ **4件とも正しく動いていた**
`ce882b4`（LINEからの追加）・`0605ad5`（つなぎ替え）・`8e64d7f`（3件の導線）を実画面で確認。
- 未ログインで `/threads-connect?add=1` → `?redirect=` 付きでログイン画面 → ログイン後に追加画面へ戻る、まで通した
- つなぎ替えボタンが `auth_type=reauthenticate` を付けていること、通常のボタンは付けないことを実測
- `/posts` は404のまま・`/post-history?status=awaiting_approval` は表示される（nextAction は後者を指している）

**ただし、日中に本番へ出した実害が本番ログに残っていた。**
9/17 18:04 JST（17:35 のデプロイの約30分後）に
`[CLIENT ERROR] Cannot read properties of undefined (reading 'default')` が1件。
スタックは**古いバンドル** `index-B4ERPFPa.js`（現在は `index-DNkQTC_8.js`）。
＝画面を開いたままのお客様が、デプロイで差し替わった部品を読めなくなった状態。
**「本番反映は夜間にまとめる」という決まりが守られていれば起きなかった。** 決まりを変えるのか、守るのかは三上様のご判断。

### 3. Moveact 予約サイト・管理画面 → ✅ **9/7から止まっていた反映が復旧**

`booking.moveact.net` は **9/7・9/10 のビルドが2回続けて失敗**し、**9/6のコンテナを11日間配信し続けていた**。
原因は `web-booking/src/components/LessonsPage.tsx` の型が `'tamashima' | 'konko'` 固定のままで、
マルチテナント対応で `StoreId` が `string` に広がったのに追随していなかった（TS2322）。

- 予約サイト：デプロイ `1655` **finished**／配信中のファイルが `index-BzZ7jml3.js` → **`index-C3nlDl1K.js`** に変わった
- 管理画面：デプロイ `1656` **finished**／`index-ZtgEtkjS.js` → **`index-B39Ux9um.js`**
- ⚠️ **ビルドが失敗しても古いコンテナが残り 200 を返す**ので、応答コードでは気づけない。
  今後も `status=finished` と**配信中ファイル名の変化**の2つで判断する。

**実画面（375px）で確認したこと**（予約の作成・お客様への送信は一切していない）:
- 店舗選択 → メニュー選択 → 担当者 → 日時 まで通した。横はみ出しなし・JSエラーなし
- **`/lessons` と `/lessons?store=konko`**（今回直した画面そのもの）が表示され、`?store=konko` で金光店に絞られる
- **メニュー相談を1回実際に使った**：送信直後に点の点滅と一緒に
  「お悩みを読んでいます。合うメニューをお選びするまで、20秒ほどお時間をいただくことがあります。」が出た（`e77b5e9`）。
  回答は **7.6秒**で届いたので、12秒・25秒の2段目・3段目の文章は出ない（仕様どおり）
- 管理画面はログイン画面が出た（ログインはしていない）

### 4. Keiro：LINEの無料通数が残りわずかなときのお知らせ → ✅ **反映（9月分は送らない状態で置いた）**

`keiro-shittoru`（`x10e9syw5oydt9pqw6hqwiij`）だけに反映。デプロイ `1657` **finished**。
`keiro`（`fstao1mijd4hgpu67j7yx7xv` = keiro.moveact.net）は**触っていない**（稼働2か月のまま）。

- 控え：`/app/data/backups/keiro-2026-09-17T17-41-09-185Z.db`
- 起動ログにエラーなし。`keiro.s-toru.com` 200
- ⚠️ **反映前に手を打った点**：新しいコードは**起動90秒後に本物のお知らせを送る**作りだった
  （`setTimeout(quotaNoticeTick, 90*1000)`）。そのまま反映すると、三上様が岡田様へご連絡される前に
  自動のお知らせが飛んでしまう。そこで**反映前に** `quota_notice_key` 列を先に作って
  モンテローザ様に `'2026-09:2'` を入れ、9月分は送らない状態にしてから反映した。
  起動90秒の回は**実際に何も送っていない**（ログに送信の記録なし）ことを確認済み。
- **他の院も先に測った**（送る前に確かめる）：Moveact 14/200（7%）・株式会社しっとる 0/200・
  たきもと新規用 0/200 ＝ **8割に届いている院はモンテローザ様だけ**だった。
- **動くことを実測した**（送信を差し替えた形・本物は飛ばしていない）：
  モンテローザ様が `notified` に **level 2 / 200通中200通**で出た。
  メールは `monteroza966@gmail.com` 宛に組まれ、**LINE送信は残り0通のため自動で見送られた**（仕様どおり）。
  実測後 `quota_notice_key` は `'2026-09:2'` に戻した。**10月1日以降は自動で動く。**
- ⚠️ **通数は 9/17 の 197/200 から 200/200（使い切り）になっていた。**
  ＝モンテローザ様のステップ配信・一斉配信は**いま実際に止まっている**。

### 5. 金光店「美容鍼＋小顔矯正 ¥9,900（60分）」を予約画面に戻した → ✅

9/4から未回答だった★3に○が出たため、**この1件だけ** `is_active` を false → true にした。

- 変更前：`id=7a515a5c-8e74-488d-81bf-a8014637345d` / `美容鍼＋小顔矯正` / `price=9900` /
  `duration_minutes=60` / `is_active=false`（`treatment_menus` テーブル）
- **値段・時間・名前は一切変えていない**（`is_active` だけ）。他のメニューには触っていない。
  ※名前の似た `肌質改善美容鍼 ¥13,200（90分）` と `美容鍼＋石膏パック ¥9,900（60分）` は**そのまま伏せた状態**
- 実画面で確認：`booking.moveact.net` 金光店 →「2回目以降のご来店」で
  **美容鍼＋小顔矯正／約60分／¥9,900 税込**が出る
- **戻し方**：`is_active=false` に戻すだけ

⚠️ **このメニューは三上様しか施術できない**（美容鍼は三上様専任）。実画面でも、このメニューの担当者は
**三上様おひとりだけ**が出る状態だった。表に戻すと**ご予約が三上様に集中する**。
10/1の移管で整体・ピラティスの【2回目以降】を久一さんへ渡す話と**逆向き**なので、
**判定日 2026-10-17** に「何件入ったか」「三上様の負担がどう変わったか」を見て続けるか下げるかを決める。
`moveact-marketing/判定待ち台帳.json` に追加ずみ。

### 6. サーバーのディスク掃除（保存期間 7日 → 2日） → ✅ ただし**効きは小さかった**

- **41% → 39%**（空き 57G → 58G）。イメージ 58本 → 54本・**回収 347.9MB**
- **ビルドキャッシュは 0B**（106本すべて直近48hに使われていた＝3.365GB はそのまま残る）
- 容量を食っているのは**稼働中49コンテナが使っている現役イメージ 26GB**で、保存期間を縮めても消えない。
  **70%に近づいたときは prune ではなくディスク増設で判断する**のが正しい。`docs/scaling-thresholds.md` に追記ずみ。

---

## 三上様の判断待ち（送信していません）

### ~~A. 小林武晴様の「強み・実績」の言い換え~~ → **2026-09-16 取り下げ（送らないことに決定）**

9/15 三上様指示「健康表現ガードで引っかかるものは最初から記載しない。クライアントが設定のところに
入れていても同じです」により、**こちら側で書かせない作りに変えた**（2026-09-16 未明に反映）。
お客様に「強み・実績」を書き直していただく必要はなくなったので、この文面はお送りしません。

### B. 佐々木竜也様（9/9ご登録・pro）の登録内容が業種と食い違う

- 業種「コンサルタント」に対し、お客さん像に「デスクワークの会社員」、強みに「専門特化（〇〇専門）」と
  治療院の言葉が入っている。9/13の朝の点検にも同じズレが出ている。
- 業種ズレの重複通知は止まっている（`industryMismatchNoticeKey`）。
- 文面は用意ずみ。**三上様の承諾をいただいてから**お送りします。

### C. Coolifyの環境変数 `RESEND_FROM_DOMAIN=resend.dev` を消す（運営作業）

- 本番の実行中コンテナは `threads-studio.com` を正しく読めている。重複しているのは
  **プレビュー用（`is_preview: true`）の1件**（uuid `nioepcaeeyhgczsx70vyhydx`）。
- 9/9 01:19 のデプロイ中に実際にこちらが拾われ、お客様宛メールが1通落ちている。
  環境変数の削除は本番設定の変更なので夜間整備では触っていない。

### D. 自動投稿の必須条件を減らすか（2026-09-11）

- 「はじめの設定」は5問で終わるのに、自動投稿は今も **お客さん像（target）と強み（strength）** を必須にしている
  （`autoPostScheduler` の `eligibleProjects`）。
- 9/13の通し確認でも同じ：5問を終えた直後は target と strength が必ず空になる。
  案内は「あと2問だけ答える」で1本道になっており、進み具合の見出しも「（あと2問）」に直した（9/13）。
- 残るご判断：必須条件そのものを「業種・地域・お悩み」まで減らすと5問だけで投稿が始まるが、
  強みが無いぶん投稿は弱くなる。**要ご判断**（夜間整備では仕様は変えていない）。

### E. 投稿を「問いかけ」で締めるのをやめるか（2026-09-13）→ **2026-09-14：数え直したら根拠が消えた**

直近**30日**の評価付き33本で数え直した（前回は22本だった）。

| | ◯ | ✕ | ◯の割合 |
|---|---|---|---|
| 問いかけで終わる | 3 | 9 | 25% |
| そうでない | 6 | 15 | 29% |

**差はほぼ無い。問いかけ締めをやめる根拠にはならない**ので、このご判断は取り下げてよいと考えます。

代わりに分かったこと：**✕24本のうち17本（71%）が、たった2名に集中**していた。

- **香取様（userId 3500）：評価8本すべて✕。** 悩みを3行（スポーツのケガ／慢性の腰痛・膝痛・首肩／交通事故）
  登録されているのに、8本とも「整形外科で11年」＋「痛む場所だけ触っても根本は変わらない」だった。
  ご本人の「ここ数日は同じ内容でした」（supportQuestions #30）はこれが正体。
  → **2026-09-14 に直した。** 登録された悩み・強みが複数あるときは、今日の1本で取り上げるものを
  日替わりで1つ指定する（`shared/topicRotation.ts`）。材料はもともと全部渡っていたのに、
  毎回いちばん上の1つだけが使われていた。**悩みが2行以上ある8名**（小西・滝本・小林・三上・氷見・森・香取・梅原の各様）に効く。
- ~~**岩根様（userId 5443）：登録内容そのものが薄い**~~ → **2026-09-18 訂正。薄くありませんでした（下の H を参照）**
- （以下は9/14時点の記録）**岩根様（userId 5443）：評価9本すべて✕。** こちらは登録内容そのものが薄い。
  悩みは「敷居が高いと思われている？」の1行だけ、強みは文章が改行で折り返された1かたまり
  （お茶・お琴・踊りをされている方に…という良い材料が、文の途中に埋もれている）。
  日替わりの指定は**あえて掛けない**（「今日使う強みは『購入して頂いて』」のような無茶な指示になるため）。
  → **お客様に材料を足していただくのが本筋。三上様のご判断をお願いします**（送信していません）。
  お聞きするとよいこと：着物のお悩みをあと2〜3つ（サイズ直し／保管・虫干し／値段が分からない 等）、
  お茶・お琴・踊りそれぞれのお客様に何を喜ばれているか。

### F. サーバーのディスク掃除 → **2026-09-15 未明に実施。急ぎではなくなった**

- 9/15 01:20 時点で **44%（41G/99G）**。9/14 の 55% から下がっており、注意ライン70%には遠い。
- 指示どおり `docker image prune -af --filter until=168h` と `builder prune` を流したが、
  **回収できたのは 18.9MB だけ**だった。理由は単純で、毎晩デプロイしているので
  積み上がったイメージが**どれも7日より新しく、168時間の条件から外れる**ため。
- いま `docker system df` では **イメージ 30.3GB のうち 20.8GB が回収可能**。
  回収したければ条件を短くする（例：`until=48h`。稼働中のコンテナのイメージは消えない。
  切り戻しは Coolify が git から作り直すので古いイメージは要らない）。
  ただし**保存期間を変えるのは運用方針の変更**なので、夜間整備では触らず 168h のままにしてある。
  **ご判断**：48時間に縮めてよければ、次の夜に流して 20GB ほど空けます。
- 注意ライン70%・変える基準「掃除しても70%超」は `docs/scaling-thresholds.md` に追記ずみ（9/14）。

### G. Keiro の決済通知の接続 → ✅ **2026-09-17 06:20〜06:25 に完了**

三上様から 2026-09-17 に「すべて進めてください」の承諾をいただき実施した。
コンテナの再起動を伴わない作業（登録とDBの読み書きのみ）なので、夜間を待たずに行った。

- 控えを取得：`/app/data/backups/keiro-2026-09-16T21-22-58-851Z.db`
- **通知の送り先を登録＝3件になった**（`keiro.s-toru.com/webhook/univapay`・id `11f1b214-…`）
- コードは **9/17 01:41 に自動反映済み**（`f6a5314`）。本番コンテナに「他事業は黙って受け流す」処理が入っていることを確認
- **モンテローザ様は着手前からDBにあった**（status=active・初回請求 2026-10-13）。
  `adoptOrphanSubscriptions` は `checked=322 / adopted=0`＝**取りこぼしなしが確認できた**という意味
- **でみず様は suspended のまま**（交通事故の入金で復活していない）
- **たきもと鍼灸整骨院 新規用＝しっとる保有に変更**（三上様「先方の枠は弊社が持ちます」）。
  `trial_ends_at` を 2026-10-08 → 2126-08-10（Moveact 玉島店・金光店と同じ値）
- `findLapsedTenants` ＝ テスト店舗1件のみ。お客様の院はゼロ

---

### H. 岩根様は「材料が薄い」のではなく、**良い材料が1本も使われていなかった**（2026-09-18・訂正）

E に「登録内容そのものが薄い。材料を足していただくのが本筋」と書いたのは**誤りでした**。実データを見直しました。

岩根様の登録内容（本番）:
| 欄 | 中身 | 日替わりに使えるか |
|---|---|---|
| 主な悩み | 「敷居が高いと思われている？」 | 1行のみ → 使えない |
| 強み | 文が改行で折り返されている | 項目に分けられない → 使えない |
| **N1顧客像** | **園遊会／文化勲章の授賞式／講演会で登壇／海外訪問／お茶席／お琴の発表会／同窓会 の7行** | **7項目に分かれる** |
| USP | 4項目 | 分かれる |
| ターゲット | 6項目 | 分かれる |

これだけ材料があるのに、投稿は30日間ずっと「敷居が高い」＋「本物の正絹」＋「創業130年」の繰り返しでした
（✕13本・○1本。9/15・9/16 も✕）。**日替わりの指定（`topicRotation`）が「悩み」と「強み」にしか無く、
N1顧客像は全体をそのまま渡していたため、毎回いちばん上だけが使われていた**のが原因です。

→ **2026-09-18 に直しました。** N1顧客像も日替わりの対象にしました（`focusN1`）。
　翌朝の投稿から、7人のお客様を1日ずつ順に取り上げます。**お客様に何かをお願いする必要はありません。**
　テストは `server/topicRotation.test.ts`。

### I. 株式会社プレステージ様は、本当に材料が足りない（2026-09-18・ご判断のお願い）

同じように調べたところ、こちらは**どの欄も1項目**でした（悩み「エステサロンで働きたい」／
USP「技術力・創業年数の長さ」／N1顧客像「美容が好き/人にマッサージするのが好き/キレイになりたい」）。
✕5本・○1本、文体のお手本も14日間空のままです。上の直しはこの方には効きません。

お聞きするとよいこと：**実際に入社された方のお話**（未経験から何年でどうなったか、入社のきっかけ、
働いてみて意外だったこと）を2〜3。採用の発信なので、働く方の生の言葉がいちばん効きます。
**文面は用意していません。送ってよいかを含めてご判断をお願いします。**

### J. 森正一郎様（4851）＝いま投稿が止まっています（2026-09-18・朝いちばんの対応）

ご要望 #36「交通事故専門の新規アカウントへ変更したい」の方です。
- つなぎ替えの直し（`0605ad5`）は 9/17 日中に本番へ出ており、**ご要望はすでに叶っています**
- ただし**ご本人はまだ気づいておられない可能性が高い**（お返事をしていないため）
- **返信の文案は用意しました。三上様の○をいただいてからお送りします**（下のK）
- いま連携は0件・9/16以降の投稿はゼロ。9/17の予約1本は失敗（下のL）

### K. 森様への返信の文案（未送信・承諾待ち）

> ご要望をありがとうございます。「連携を解除しても、もう一度つなぐと元のアカウントに戻ってしまう」
> という点、こちらの作りの問題でした。**9月17日に直してあります。**
>
> いま「Threads連携」の画面を開くと、緑の「Threadsと連携する」ボタンのすぐ下に
> **「別のアカウントにつなぎ替える（Threadsのログイン画面からやり直す）」**という文字が出ています。
> こちらを押していただくと、Threadsのログイン画面が必ず開きますので、
> 交通事故専門の新しいアカウントでログインし直してください。
>
> 公式LINEのメニュー「Threadsアカウントを追加」から開いていただくと、この画面がすぐ出ます。
> （パソコンからの操作が確実です）
>
> ご不明な点があれば、このままトークにお送りください。

### L. 直した：連携解除ずみのアカウントに、投稿を公開しに行っていた（2026-09-18）

森様の投稿 #1573（9/17 11:02）が
`Failed to create media container: Invalid OAuth 2.0 Access Token` で失敗していました。

原因：連携解除（`deleteThreadsAccount`）は `isActive=0` にしてアクセストークンを空にするだけで、行は残ります。
公開の処理は `getThreadsAccountById` で取りますが、この関数は `isActive` を見ないため
「アカウントが見つからない」の分岐をすり抜け、**空のトークンのまま Threads へ公開しに行っていました**。

→ 連携解除ずみなら公開を試みず「見送り」にし、理由を日本語で残すようにしました。
　テストは `server/scheduledPostUnlinked.test.ts`。

---

## 2026-09-16 未明に直した分

### 1. 健康表現を「最初から書かせない」（9/15 三上様指示・3点とも反映）

- **結果を語る切り口を、健康系のお店では日数にかかわらず常に外した**（`autoPostScheduler` の
  `excludeOutcomeAngles`）。これまでは連携10日未満だけで、11日目に戻したとたんに結果表現が出ていた。
- **はじめの設定の文字列を、プロンプトへ渡す前に洗うようにした**
  （`shared/healthClaimGuard.ts` の `scrubSettingText` / `scrubSettingList`。
  適用は `generateThreadsPrompt` の中なので、毎日の投稿・固定投稿・手動生成のどれも同じように洗われる）。
  対象は 強み・実績・お客さん像・USP・N1顧客像・主張・決めゼリフ・お客さんの生の言葉と、
  カウンセリングの実績・エピソード・お客様の変化・よくある質問・要旨の「どんな未来へ」。
  **「主な悩み」だけは洗わない**（投稿の主題そのもので、落とすと題材が無くなる。本文側の検査は残してある）。
  洗ったときは `[AutoPost] はじめの設定から健康の断定を除外 userId=…` がログに出る。
- 本番の実データで確かめた：userId=2907 の強みからは「手術宣告…症状改善したり／杖がいらなくなったり」が、
  5002・556 からは「根本改善にこだわる」が、5002 の N1顧客像（不妊→妊娠出産）は丸ごと落ちる。
  **実際に3本作らせて、3本とも健康表現のガードを通った**（洗う前は3本中1本が「手術を回避できた方もいます」で止まっていた）。
- ついでにガードの取りこぼしを足した（緩めてはいない）。
  「手術を回避できた」「杖が要らなくなり」「痛みが和らぎ」「治ってました」「健康回復できました」。
  直近30日の健康系の公開投稿749本で試すと**新たに止まるのは6本だけで、すべて userId=2907**だった
  （＝いまも本番に出ている表現）。
- ルール文書 `docs/safe-operation-rules.md` に追記ずみ。テストは
  `server/healthClaimGuard.test.ts` と `server/healthClaimProfile.test.ts`。
- **洗うと材料が痩せる方を毎晩見られるようにした**：`npx tsx scripts/ops/health-scrub-audit.mts`
  （本番DBへのトンネルを張って実行）。9/16 未明の結果：
  髙木様の「主張・信念」と小西様の「N1顧客像」は**丸ごと空**になり、
  氷見様（2907）と梅原様（3521）の「強み」は**半分以下**になる。
  とくに梅原様は「間違った身体の使い方を改善する運動療法」という良い材料が
  「改善する」を含むために落ちるので、**材料を足していただくのが本筋**（岩根様と同じ性質）。
  ガードを緩める方向では直さない。→ 三上様のご判断へ（下の E とまとめて）。

### 2. 「あと2問だけ答える」で、2問目のお答えが消えていた（新規のお客様の一本道）

通し確認で再現した、いちばん重い不具合。

- 「次にやること」は5問を終えた方に「お客さん像と強みだけ空いています。**2問にお答えください**」と案内するのに、
  1問答えた時点で会話が終わっていた（`moreOne`）。お客様が続けて2問目を送っても、はじめの設定の続きとしては
  受け取られず、**ご質問として自動応答に流れてお答えがそのまま消えていた**（強みは空のまま）。
  自動投稿はお客さん像と強みが揃うまで始まらないので、ここで止まると**その方の投稿はゼロのまま**。
- 必須項目が残っているあいだは続けて聞くようにした（`CounselingState.gating`）。
  ローカルQAで 2問→1問→「これで自動投稿の準備がそろいました」まで通し、DBに両方入ることを確認。
- あわせて、案内した項目と違う質問が出る道も塞いだ（列は空なのに、はじめの設定の答えだけ残っている場合）。
- テストは `server/counselingGating.test.ts`。

### 3. 慣らし運転の説明が、自動応答の知識に1行も無かった（ご質問 #33 の原因）

- 小西様（5002・pro＝1日3件）の「1本ですよ？」に自動応答が答えられなかった（`aiConfident=0`）のは、
  `shared/productKnowledge.ts` に慣らし運転の説明が**まったく無かった**ため。
- 日数・本数・理由・取り戻し・ライトは対象外・Threads歴の長いアカウントは対象外、を書き足した。
  数字の出どころは `shared/accountRamp.ts`（推測では書いていない）。
- **実際にAIに聞き直して確認した**：「1本ですよ？」「3件で契約しているのに1件しか投稿されていません」
  「減った分はどうなるの？」の3つとも正しく答える（`confident=true`）。
  「いつから3件になりますか」は最初アカウント連携数の話と取り違えたので、知識に読み分けを書いて直し、再確認ずみ。
- テストは `server/threadsAppKnowledge.test.ts`。

### 4. 「投稿が消された」ときのことが、自動応答の知識に1行も無かった

R10 を直す過程で気づいた。**冷却期間（7日・1日1件）のことが `shared/productKnowledge.ts` に一切なかった。**
お客様には「投稿が消されたため◯/◯まで1日1件に抑えています」とお送りしているのに、
その後お尋ねがあっても自動応答は答えようがない状態だった。

- 書き足した内容（数字の出どころは `shared/accountRamp.ts` の `COOLDOWN_DAYS=7` と
  `server/accountHealthJob.ts`。推測では書いていない）：7日間・1日1件・自己返信とリンクコメントなし・
  冷却中は補填なし・こちらでは元に戻せない・冷却明けに1日＋1件で返す・ライトプランにも掛かる・お客様の操作は不要。
- **取り違えを1つ潰した**：「1日1件に抑えると言われました。いつ戻りますか」は
  慣らし運転（11日目）と冷却期間（7日）のどちらにも読めるので、読み分けを書いた。
  書く前は**慣らし運転と決めつけて答えていた**（実測）。書いたあとは、
  「消された」「◯/◯まで」が入っていれば冷却、「連携したばかり」なら慣らし、
  手がかりが無ければ慣らしで答えたうえで冷却の可能性を一言添える、と正しく分かれる。
- **実際にAIへ投げ直して確認ずみ**（8問。うち「消えた分は返してもらえますか？ライトプランです」は
  「冷却明けから1日2件でお返しします」と正しく答える）。
- テストは `server/threadsAppKnowledge.test.ts`（6件追加。取り違えの実例を文字列で固定）。

### 確認できたこと（問題なし）
- 会員登録 → ログイン → 料金プラン → **有料4プランすべて**、ポップアップを遮断した状態でも同じタブで
  決済ページ（checkout.gopay.jp）まで進める。金額も 4,980／9,800／29,800／55,000 で一致。JSエラーなし。
- 375px の主要22ページに、横スクロール・JSエラー・空表示なし。
- LINEのボタン83種すべて応答あり（無反応・「うまく受け取れませんでした」・未連携の案内はゼロ）。
- 「はじめの設定（まず5つ）」は 1／5 → 5／5 → 確認 → 登録 まで通る（9/15 の直しの回帰確認）。
- 固定投稿は 作る → 品質検査 → 案 → ピン留めの手順案内 → 記録 まで通る。
- `npx vitest run` は 103ファイル・830件すべて成功。

---

## 2026-09-15 未明の通し確認で見つけて直した分

ローカルQA（port 3100）で、新しいお客様の目線でひととおり操作した結果。

- [x] **「はじめの設定」の数が、2問目で戻って見えていた**。公式LINEでは先にホームページのURLを聞き、
      その画面だけ「【1／5】」と書いてある。ところが次の質問から `questionsFor(..., quick)` が返す
      **4問で数え直す**ため「【1／4】」に戻り、お客様からは **1／5 → 1／4 → 2／4 → 3／4 → 4／4** と、
      数が減ってから進むように見えていた。いちばん最初に通る道で、ここで「あれ？」と思わせると
      そのまま離脱になる。URLを1問目として数え、**2／5 → 5／5** で終わるようにした
      （`askQuestion` の `urlStep`）。やり直し（全20問）は今までどおり 1／20 から。
      実機で 1／5 → 2／5 → 3／5 → 4／5 → 5／5 → 確認画面 →「この内容で登録する」→
      「次にやること（あと2問）」まで通ることを確認。テストは `server/counselingProgress.test.ts`。
- [x] **品質ガードの2つが、止めた理由を作り直しに渡していなかった**。
      `voiceGuard`・`identityGuard`・`naturalnessReview`・使い回し判定は
      `lastRejectReason` に理由を入れて次の回に渡すのに、**`healthClaimGuard` と
      `fabricatedNumberGuard` だけ入れていなかった**。そのため生成側は何が悪かったか分からず、
      同じ言い回しを作り直していた。9/14 の本番ログで **userId=2907 が「短時間で楽になる約束」で
      24時間に5回**、さらに「本文が短くなりすぎたため公開しない」で**3枠**が消えている。
      **ガードは緩めず**、`healthClaimRetryHint` / `fabricatedNumberRetryHint` を足して
      「何が止まったか・どう書き直すか」を次の回に渡すようにした。
      あわせて、理由なしで出ていた警告ログに userId を付けた（誰の枠が消えたか追えなかった）。
- [x] **自動応答の知識が「アプリからの登録は全20問」と言っていた**（実物と食い違い）。
      アプリの画面（`/ai-counseling`）は **2026-09-10 から公式LINEと同じ4問に揃っている**
      （`AICounseling.tsx` が `quickQuestions(allQuestions)` を使っている。テストでも固定ずみ）のに、
      `shared/productKnowledge.ts` だけ「従来どおり全20問です」のまま残っていた。
      これをそのままお客様にご案内すると、実物より重く見えて手が止まる。
      実物に合わせて直し、**進み具合が「1／5」から「5／5」まで進むこと**と
      **やり直しのときだけ全20問**も書き足した。
      直したあと、実際にAIへ3通り聞き直して正しく答えることを確認した
      （「はじめの設定って何問？」「アプリからだと20問ですか？」「設定が長そうで手が止まっています」）。
      資料側（スタートガイド・ご登録ガイド・LP）は**もともと正しかった**ので直していない。

- [x] **QAのLINE検証アカウントが、消えたユーザーを指したままだった**（ローカルのみ）。
      `Unightqa0903test` の紐づけ先 userId=3030 がローカルDBに存在せず、
      ボタン83種の総当たりが**全部「ご利用にはアカウントの登録が必要です」で返っていた**。
      つまり「83種すべて応答あり」は**何も確かめられていなかった**。
      紐づけ先を生きているQAユーザー（2927）に直してから流し直し、
      **83種すべてで無反応・THROW・「うまく受け取れませんでした」ゼロ**を確認した。
      → 次の夜も、まず**返事の中身が「未連携の案内」になっていないか**を見ること。

### 確認できたこと（問題なし）
- 会員登録 → ログイン → 料金プラン → **有料4プランすべて**で、ポップアップを遮断した状態
  （`--block-new-web-contents`）でも**同じタブで決済ページ（checkout.gopay.jp）まで進める**。
  金額もライト4,980／プロ9,800／ビジネス29,800／代理店55,000で一致。JSエラーなし。
- 375px で主要22ページに、ページ全体の横スクロール・JSエラー・空表示なし（ログイン後も同じ）。
- 会員登録の「アカウント作成」ボタンは、Cookieのお知らせが出ていても覆われない（9/13 の直しの回帰確認）。
  ※前回「覆われている」と出たのは**確認スクリプト側の誤り**で、`elementFromPoint` が返す
  **祖先**要素を「別の要素」と数えていた。`top.contains(b)` も見るように直した。
- 固定投稿：作る →（品質検査で作り直し1回）→「これで投稿する」→ 公開予定 → ピン留めの手順案内 →
  「ピン留めしました」の記録まで通る。**2本目以降はピン留め案内が出ない**のは仕様どおり
  （そのアカウントで確認済みだから）。前回「案内が出ない」と見えたら、まず
  `threadsAccounts.pinnedPostConfirmedAt` を見ること。
- `npx vitest run` は **101ファイル・801件すべて成功**（申し送りにあった「既存の失敗46件」は解消済み）。

---

## 2026-09-15 朝に見ること（今夜の反映分）

本番は **`97843a1`** が稼働（01:41 切替・health 200・起動ログにエラーなし）。
今夜は2回反映した（`bb933a0` 01:31 → `97843a1` 01:41）。どちらも自動反映が拾っており、
手で `api/v1/deploy` は叩いていない。マイグレーションの追加は無し。

- [ ] 6:00 の生成ログで、**`healthClaimGuard` / `fabricatedNumberGuard` で落ちた枠の
      作り直しが1回で通っているか**。ログに userId が出るようになったので、
      `grep 'healthClaimGuard' | grep -o 'userId=[0-9]*' | sort | uniq -c` で数える。
      **userId=2907 の「短時間で楽になる約束」が5回→減っているか**がいちばんの見どころ。
- [ ] 同じく「本文が短くなりすぎたため公開しない」が減っているか（9/14 は3枠が消えた）
- [ ] 新しくご登録の方がいたら、公式LINEの「はじめの設定」が
      **1／5 → 2／5 → 3／5 → 4／5 → 5／5** と進んでいるか（戻って見えないこと）
- [ ] 自動応答に「はじめの設定は何問？」系のご質問が来たら、
      **「アプリからでも4問」** と答えているか（9/14 までは「アプリは全20問」と答えていた）

---

## 2026-09-17 公式LINE→Webの遷移の総点検（12件）

三上様ご指摘「公式LINEのアカウント追加を押してログインしても目的の画面に行かない。
表示と違うページに飛ぶところが他にないか、こと細かく」から総点検。12件見つかった。

### 日中に直して反映した3件（三上様の承諾を得て 2026-09-17 夕方に反映）

- [x] **①「Threadsアカウントを追加」を押すと、必ず料金ページに飛んでいた**。
      `ThreadsConnect.tsx` はご契約の内容を読み終わる前に描き始めるため、その一瞬だけ
      「連携できる数＝0」と見えて「プランを選んで連携を始める」（料金ページ行き）が出ていた。
      押せてしまう位置なので、実際にそこへ飛ばされた方がいる。
      → 契約の内容が読めるまで画面を出さない。読めなかったときも0と決めつけず、読み直しを出す。
- [x] **②「新規登録」に寄り道すると、戻り先（`?redirect=`）が消えていた**。
      公式LINE →ログイン画面→新規登録→ログイン、と回ると最後に目的の画面へ戻れなかった。
      → `新規登録`リンクと登録後のログイン画面に `redirect` を引き継ぐ（コード類も一緒に）。
- [x] **③「固定投稿をThreadsに公開する」の行き先が存在しないページ（`/posts`）だった**。
      → `/post-history?status=awaiting_approval`（承認待ちの一覧）に変更。

### 今夜の整備で直す残り（④〜⑫）

- [ ] ④〜⑫ は当日の点検メモのとおり。日中の実害が続いていないものをここに回した。

---

## 2026-09-17 未明に直した分

### 1. R9・R10・R11（上の「2026-09-16 追加」の節を参照）

### 2. ご自身のお店を紹介する文章が、ご質問として自動応答に回っていた（ご質問 #34）

氷見様（2907）が 9/15 に送られた
「滑川市では、お子様から90代まで触れるだけの小波津式でケアしています😊／施術で不安なことはありますか？」に、
自動応答は **「こちらはどのようなご質問でしょうか？」としか返せなかった**（`aiConfident=0`）。

- 原因は知識の不足ではなく**仕分け**。投稿文は読み手への問いかけで締めることが多く、
  `isPastedContent` は「？」がある文章を **飾り＋3行以上＋60字以上** のときだけ投稿文とみなす。
  この文は **2行・約52字**で、条件から外れてご質問として自動応答に回っていた。
  行数・長さのしきい値を下げるとふつうのご質問まで巻き込むので、そこは触っていない。
- 代わりに**ご登録の内容と突き合わせて**判断するようにした（`shared/requestKind.ts` の
  `ownPlaceMarkers` / `looksLikeOwnPostMaterial`）。ご登録の地域・店名が入っていて、
  アプリのこと（投稿・設定・連携・プラン…）に触れていなければ、それはこちらへのご質問ではなく
  ご自身のお店の紹介文。氷見様のご登録は「富山県滑川市上小泉1818-1」なので「滑川市」で当たる。
- **実際にAIへ投げ直して確かめた**（ローカルQAに同じご登録内容を作って6通り）：
  #34 そのまま・絵文字なしの紹介文 → 「実績として登録」のご案内。
  「滑川市の投稿が今日は届いていません」「よくなる整体院のアカウントを連携したいのですが」
  「プロプランは1日に何件」「3件で契約しているのに1件しか」→ これまでどおり正しくお答えする。
- テストは `server/requestKind.test.ts`（#34 の文章をそのまま入れてある・6件追加）。
- **本番のご質問30件すべてに新しい仕分けを当てて確かめた**（横取りしていないかの実測）。
  当たったのは3件だけで、**3件とも氷見様（2907）のお店の紹介文**。うち #7 は従来も投稿文と
  判定ずみ（挙動は変わらない）、#6 と #34 は「答えられなかった」ものが正しく材料に回る。
  **これまで自信をもってお答えできていたご質問を横取りしたものは0件。**

### 3. ご質問 #35（梅原様「これはどちらのアカウントですか？」）

R12・R13（承認カードにアカウント名を出す）で 2026-09-16 日中に反映ずみ。本番は `470a62d` が稼働中。
自動応答自体は `aiConfident=1` で答えられていたので、知識は足していない。

### 確認できたこと（問題なし）
- 会員登録 → ログイン → 料金プラン → **有料4プランすべて**、ポップアップを遮断した状態
  （`--block-new-web-contents`）でも同じタブで決済ページ（checkout.gopay.jp）まで進める。
  金額も 4,980／9,800／29,800／55,000 で一致。JSエラーなし。
- 375px の主要22ページに、横スクロール・JSエラー・空表示なし。
- LINEの83種すべて応答あり（**未連携の案内・「うまく受け取れませんでした」・無反応はゼロ**）。
  ※検証用LINEは `Uqa_r9_scope`（userId 6492・ローカル）。返事の中身が未連携の案内でないことを毎回数えること。
- 「はじめの設定（まず5つ）」は 1／5 → 5／5 → 確認 →「登録する」まで通る。
- 固定投稿は 作る → アカウント選択 → 品質検査（healthClaimGuard が「治る・改善の断定」を1件落とした）→ 案 まで通る。
- `npx tsc --noEmit` エラー0／`npx vitest run` は 106ファイル・857件すべて成功（失敗0）。

### 本番への反映（2026-09-17）

`fb64474` が 01:36 に切り替わり（health 200・起動ログにエラーなし・マイグレーションの追加なし）。
自動反映が 01:30 に拾っており、手で `api/v1/deploy` は叩いていない。
本番のイメージの中身で、今夜の5点がすべて入っていることを確認した
（`dist/index.js` は日本語が `\uXXXX` なので逃した形で grep）。
`/pricing?coupon=SEMINAR2026` を含む本番6ページを375pxで開き、横はみ出し・JSエラーなし。

**この行を足したあとの docs の変更は、コミットだけして push していない**（反映は1回にまとめるため）。
次の夜の `git log @{u}..HEAD` に出るので、そのときまとめて反映すること。

### 岩根様（5443）の✕が続く件 ＝ 品質ガードは効いている。材料が薄いのが原因（判断待ち E の裏取り）
直近14日の評価は **✕11件・◯1件**。9/15 の1本（id 1549）も✕。
実物の文章で確かめたところ、`findRepeatedHookNumber` は「130年」を、`findRepeatedPhrase` は
「か岡山で創業130年の」を**どちらも検知できている**。それでも公開されるのは、
**どちらも「最後の作り直しでは止めない」（枠を捨てない）作り**だから。
＝ 何度作り直しても同じ材料しか無いので、毎回最後の作り直しまで行っている。
**ガードを緩める／厳しくする話ではなく、材料を足していただくのが本筋**（判断待ち E のとおり）。

---

## 恒常のメモ

- LINEの月間通数はコミュニケーションプラン（5,000通）。朝の点検に残量が出る（残り1割で警告）。
  通数切れのときは、お客様へは同じ内容がメールで届き、運営にも1日1回知らせが飛ぶ。
- ローカルQAは port 3100（`QA_SAFE_MODE=1`）。DBはローカルMySQLなので本番には触れない。
  ポートが埋まっていると勝手に 3101 へ逃げるので、確認の前に**どちらを見ているか必ず確かめる**。
  `handlePostback` / `handleFreeText` を直接叩く確認は、`BUILT_IN_FORGE_API_KEY` と `BUILT_IN_FORGE_API_URL`
  を Coolify の env から渡さないと自動応答が動かない（`.env` には入っていない）。
- LINEの総当たりは `scripts/ops/line-sweep.mjs`。**値を取るボタンは必ず値を付けた形で書くこと**
  （`c=st` のように前半だけ書くと、アプリは正しく「うまく受け取れませんでした」と返すので、
  こちらの書き間違いが不具合のように見える。2026-09-13 に一度そう見誤った）。
- ⚠️ **マイグレーションの適用済み判定はファイル名だけ**（`server/_core/index.ts`・中身のハッシュは見ない）。
  一度本番に当たったマイグレーションのファイルを後から書き換えても、**二度と実行されない**。
  直したいときは必ず新しい番号のファイルを足すこと。
  （0085 は本番未適用のうちに書き換えたので影響なし。ローカルだけ古い版が当たっていた）
- ⛔ **本番コンテナの中で `node -e 'require("/app/dist/index.js")'` を絶対にやらないこと**（2026-09-15 にやってしまった）。
  `dist/index.js` はサーバーの起動そのものなので、**本番のenvでアプリがもう1つ立ち上がり**、
  定期ジョブ（自動投稿・お知らせ）も初期化される。**二重投稿・二重送信になる。**
  今回は 01:44〜01:46 の約2分で気づいて止めた（次のジョブは6:00なので実害なし）。
  中身を見たいだけなら `grep` で十分。コンテナに `ps` も `kill` も無いので、
  消すときは `/proc` を読んで `sh -c "kill -9 <pid>"`（シェル組み込み）を使う。
- ⚠️ **`dist/index.js` の中の日本語は `\uXXXX` に変換されている**（esbuild の既定が ascii）。
  反映を確かめるときに日本語でそのまま grep すると **入っていても 0 件に見える**。
  逃した文字列に直してから探すこと（例：`同じ4問` → `\u540C\u30584\u554F`）。
- auto-deploy はサーバー側の cron（5分ごと・`/opt/scripts/auto-deploy.sh`）。**push すれば勝手に反映される**ので、
  手で `api/v1/deploy` を叩くと二重デプロイになる。トークンを入れ替えたら
  `~/.claude/secrets/coolify.token` と `/opt/scripts/.coolify-token` の両方を直す。
  確認は `tail /var/log/auto-deploy.log` に `Unauthenticated` と `[FAIL]` が無いこと。

---

## 2026-09-14 朝に見ること（9/13＋9/14 未明の反映分）

昼のうちにコミットだけしてあった4件（承認なし公開・ご案内先URLの判定・スパム判定の精度）と、
夜間整備で直した5件をまとめて反映。

### 昼の分（9/12）の確認
- [ ] executor ログに `見送りなしのため公開へ: N件／日をまたいだ承認待ちを見送り: M件` が出る。
      9/13時点の承認待ちは6件（三上様の871＝8/31作成、香取様の1297・1298・1346・1353・1357）。
      **全部「日をまたいだため見送り」になるはず**（当日分だけが公開に回る仕様）
- [ ] 7:40 のお知らせ（`publish_unless_declined_2026-09-13`）が公開前確認ONの方に届く
- [ ] 固定投稿のコメントに「ふだんの様子」が Instagram 以外で出ない
- [ ] `Follow-up bump scheduled` が出ない／`inquiry comment skipped …（本日2件目以降）` が出る
- [ ] 7:40の報告で、しっとる公式に「投稿が消されたため9/19まで1日1件」の注記

### 本番に入っているもの（2026-09-14 01:59 時点）

`8fe862b` が稼働中（health 200・起動ログにエラーなし）。migration `0087` 適用ずみを本番DBで確認
（`scheduledPosts.approvedAt` / `approvedVia` / `threadsAccounts.deletedShortfall`）。

今夜は3回反映した（`925cdd0` → `c9e137e` → `8fe862b`）。2回目・3回目は、
本番の実データで確かめてから直した分（悩みの日替わり）。
**`8fe862b` は 01:45 の自動反映で一度ビルドに失敗している**（`vite build` の途中で落ちた。
直前の `c9e137e` のビルドと重なった時間帯で、ローカルでは同じコミットが通る）。
自動反映は「新しいコミットが来たときだけ」動く作りなので**失敗しても再挑戦しない**。
手で `api/v1/deploy` を叩いて入れ直し、01:58 に切り替わった。
→ **同じことが起きたときは、`/var/log/auto-deploy.log` ではなく
Coolify の deployments API で `failed` を見ること**（ログには「queued」としか残らない）。

### 9/14 未明に反映した分の確認（新しい）
- [ ] 6:00 の生成ログに `[AutoPost] account N daily cap` や `今日すでにN件（翌日へ送られた分など）` が出るか。
      **1人で毎枠**のように出すぎていないか
- [ ] 悩みが2行以上ある8名（小西・滝本・小林・三上・氷見・森・香取・梅原の各様）の投稿が、
      **前日と違う悩み**を扱っているか。とくに香取様（3500）が「11年」＋「痛む場所だけ」から抜けたか
- [ ] 承認したあと、投稿の履歴に「承認: 9/14 …  LINE（1件ずつ）」が出るか（本番で初めて値が入る）
- [ ] 固定投稿を「まず5つ」だけの方が押したときに「あと2問だけ答える」が出るか
- [ ] 7:40 のまとめで、案内OFFの岩根様にも announcements の経路が通っているか（9/14 はお知らせ無しなので空でよい）
- [ ] Keiro の `follows` に 玉島 `tnt_dbf11acd3ef4461b8157a21f` / 金光 `tnt_f990e5ee575ee176cbcde760` が1件でも入るか

### 夜の分（9/13）の確認
- [ ] `[AutoPost] 書き出しが直近の投稿と同じ実績の数字「◯年」→ 作り直し` が出る。
      出すぎ（1人で毎枠）になっていないか。**出ても最後の作り直しでは止めないので、枠は減らない**
- [ ] `[PinnedFlow] 固定投稿の品質: … → 作り直し` が出たときに、ちゃんと3回以内で通っているか
- [ ] 会員登録が 375px で最後まで押せる（Cookieのお知らせの下にボタンが隠れない）

---

## 2026-09-13 未明の通し確認で見つけて直した分

ローカルQA（port 3101）で、新しいお客様の目線でひととおり操作した結果。

- [x] **スマホで会員登録の「アカウント作成」ボタンが押せなかった**。Cookieのお知らせが画面の下に
      貼り付く（fixed・高さ276px）ため、フォームの一番下まで送っても、同意チェックのすぐ下にある
      ボタンがお知らせの下に隠れていた（375×812 も 375×667 も同じ。実測で
      `elementFromPoint` がお知らせの文章を返す）。お知らせが出ているあいだは同じ高さだけ
      ページの下に余白を足して、必ず上へ逃がすようにした（`CookieConsent.tsx`）。
- [x] **固定投稿の生成に、毎日の投稿にかけている品質検査が1つもかかっていなかった**。
      `pinnedPostFlow.ts` に `findBannedTic` も `checkNaturalized` も `healthClaimGuard` も無く、
      その場で作った1本目に「諦めていませんか？」（人の投稿には出てこない決まり文句）が入っていた。
      固定投稿はプロフィールの一番上に置きっぱなしになる、いちばん人目に触れる投稿なので、
      3回まで作り直す形で同じ検査をかけた。**作り直しても直らないときは下書きとしてお見せする**
      （手ぶらで帰さない。公開はご本人が押したときだけ）。実測で3本中1本が作り直しになり、3本とも合格。
- [x] **同じ実績の数字で書き出す投稿が続くのを止める**。香取様の「ここ数日は同じ内容でした」
      （supportQuestions #30）に対し、9/11に入れた使い回し判定（10文字の一致）は
      **実物5本を1件も拾えていなかった**（一字一句は違うため）。読む側に同じに見える正体は
      「11年」という実績の数字で毎回書き出していたこと。書き出し45字の中の数字を見る判定を足した
      （`findRepeatedHookNumber`）。本番の実投稿370本で試して16.2%が作り直しになる見込み。
      中身を読むと、acct12「猫背を悪化させる習慣、3つあります」の重複、acct14「SNS投稿が続かない
      お店の共通点、3つあります」の5回など、**止めるべきものが当たっている**。
      最後の作り直しでは止めないので、枠は捨てない。
- [x] **「明日は臨時休診です」が、何も受け取っていない返事に落ちていた**。
      9/11に香取様へ「出してほしい話題はそのまま送ってください」とお伝えしているのに、
      短い言い切りはご質問にもご依頼にも当たらず「ご用件を下から選んでください」で終わっていた。
      お知らせだと分かるものを受け取り、①今日の投稿の書き直しに入れる ②アプリの「イベント」に
      登録すると14日前・7日前・3日前・前日・当日の残っている分だけお知らせ投稿を作る、の
      2つをお伝えするようにした（`looksLikeAnnouncement`）。知識（`productKnowledge`）にも足したので、
      長い文でご相談いただいた場合も同じ内容で答える（実測で一致を確認）。
- [x] **「設定が終わりました」と言いながら、同じ画面で「□ お店の情報を登録」が未完のままだった**。
      5問を終えた方には「□ お店の情報を登録（あと2問）」と出すようにして、本文・ボタンと揃えた。

### 確認できたこと（問題なし）
- 会員登録 → ログイン → 料金プラン →「このまま無料で始める」まで、**ポップアップを遮断した状態でも同じタブで進める**。
- 375px で主要22ページに、**ページ全体の横スクロール・JSエラー・空表示なし**。
  （`/pricing` の比較表と `/post-history` の飾りは `overflow-x:auto` / `hidden` の中なので、はみ出しではない。
  QAスクリプトはページ全体の `scrollWidth` で判定するように直した）
- LINEのボタン83種すべて応答あり（無反応・「うまく受け取れませんでした」ゼロ）。
- 「はじめの設定」は5問を最後まで通せ、確認画面 →「この内容で登録する」→ 次にやること まで正常。
- 固定投稿は 作る →「これで投稿する」→ 公開予定 → ピン留めの手順案内（`n=pinhow`）→ 記録（`n=pinned`）まで通る。

---

## 前の夜から残っている宿題

### ★★ 2026-09-13 三上様決定の7ルール（R1〜R8）— 2026-09-14 未明に反映ずみ

日中にコミットしてあった実装を、ローカルQAのDBで1つずつ実際に動かして確かめたうえで反映した
（`0087` 適用・`approvedAt`/`approvedVia`・`deletedShortfall` を実データで確認）。
R1〜R8 はすべて済み。**翌朝に見ること**は下の「2026-09-14 朝に見ること」へ移した。

夜のあいだに1か所だけ直した：`nextDaySameTime` が「翌日の同じ時刻」ではなく
「公開が遅れた時刻の翌日」を返していた（10:00 の予定を 10:17 に処理すると翌日 10:17）。
毎日少しずつ後ろへ流れて、その方の bestHours から外れるので、**時刻は予定どおり・日付だけ翌日**に直した
（`shared/dailyCap.ts`／テスト2件追加）。

### 2026-09-13 追加（Moveact 両店の LINE 流入計測・Keiro）
- [x] **Keiro（keiro-shittoru, Coolify uuid `x10e9syw5oydt9pqw6hqwiij`）を再デプロイする。**（9/14 00:09 済み・控え keiro-2026-09-13T15-07-01-789Z.db・合言葉ヘッダ付き転送で webhook_last_at 更新を確認） GitHub `shittoru-ad/keiro` は push 済み（webhook: KEIRO_FORWARD_TOKEN）。環境変数 `KEIRO_FORWARD_TOKEN` は Coolify に登録済み。
      VPS で `curl -H "Authorization: Bearer $(cat ~/.claude/secrets/coolify.token)" 'http://localhost:8000/api/v1/deploy?uuid=x10e9syw5oydt9pqw6hqwiij&force=true'`（トークンはローカルの secrets から渡す。VPS上に書かない）。
      デプロイ後の確認：`https://keiro.s-toru.com/` と `https://line.moveact.net/` が 200／コンテナログに `webhook received before channel secret is set` が **出なくなる**／
      Keiro DB `follows` の tenant `tnt_dbf11acd3ef4461b8157a21f`（玉島）`tnt_f990e5ee575ee176cbcde760`（金光）に翌日以降 1 件でも入る。
      デプロイ前に `docker exec <keiro> node scripts/backup.js` で控えを取る。
- [x] Threads Studio の Moveact 2プロジェクトの `ctaLink` は `lnk_ma_tama_threads` / `lnk_ma_konko_threads` で確認ずみ（2026-09-14 未明）。
      Keiro 側も 玉島 `tnt_dbf11acd3ef4461b8157a21f` / 金光 `tnt_f990e5ee575ee176cbcde760` とも `webhook_last_at` が 9/14 00:09 に更新されており、
      `webhook received before channel secret is set` は**その 00:09 の4件が最後**（合言葉が入る前に届いた分）。以降は出ていない。
- [ ] 残るのは翌朝の確認だけ：
      6:00 生成後の「その日1件目のリンクコメント」がこのリンクで出ていることを確認（Keiro の clicks に `_threads` が増える）。

### 2026-09-16 追加（梅原様の「お店の情報が上書きされた」件で見つかった3つ）→ **2026-09-17 未明に3件とも反映**
- [x] **R9 「はじめの設定」のやり直しが、既存のお店の情報を黙って上書きする。**
      **真因が分かった。**「アカウントが2件以上あれば選ばせる」入口（`m=setup`）は 2026-09-01 から入っていた。
      抜けていたのは**そのあと accountId を落とす道が2つあった**こと。
      ① 途中のままの設定に出す「最初からやり直す」ボタンが `c=start&mode=…&fresh=1` で、**a= を付けていなかった**
      ② 1問目で「戻る」と送られたときの `startCounseling(lineUserId)` に accountId を渡していなかった。
      accountId が落ちると `c=start` 側は「既存の1件目のプロジェクト」を書き換え先に選ぶ。
      梅原様は 9/15 に @yusuke_seitai の設定を途中まで進め、9/16 8:18 に「最初からやり直す」を押されている。
      だいご接骨院が置き換わり、@yusuke_seitai への紐づけが空のままだったのは**両方ともこの1つの原因**で説明がつく。
      直した：両方で accountId を持ち回る＋最後の受け皿として、`c=start` に a= が無く連携が2件以上あれば
      その場でアカウントを選び直していただく（既存を書き換え先にしない）。
      テスト `server/counselingAccountScope.test.ts`（5件）。ローカルQAで、2件目の設定を途中でやめ→やり直し→
      5問完了まで通し、**1件目が無事・2件目に新しいプロジェクトが作られて紐づく**ことを実機で確認。
      復元は 2026-09-16 に実施済み（3:00のバックアップから復元＋ダイエット用を新規作成 o0ufwq4xH_w9bMwYUqCLp）。
- [x] **R10 消された投稿の補填が、連携30日以内のアカウントに掛からない。**
      `server/accountRampCheck.ts` の `base0.days >= COMPENSATION_WINDOW_DAYS` の条件を外した
      （冷却中・慣らし運転中に増やさないのは従来どおり）。
      **ライトプラン（契約1日1件）は `compensationCount` が `want < 2` で掛からない**ため、
      これまでは30日目まで1件も戻らなかった。いま該当するのは香取様の @shin_honetugi（`deletedShortfall=1`・
      9/21まで冷却）で、冷却明けから返る。
      **二重に返る道も塞いだ**：連携30日以内は慣らしの補填が同じ不足を拾っているので、
      本数は増やさずに `deletedShortfall` を消化だけする（塞がないと30日を過ぎてからもう一度返っていた）。
      テスト `server/deletedShortfall.test.ts`（7件）。ルール文書 `docs/safe-operation-rules.md` R6 に追記。
      ※梅原様の @yusuke_seitai は `deletedShortfall=0`・手当ての `extraPostsPerDay=1`（9/25まで）が
      　入っているので、この直しによる二重の補填は起きない（本番DBで確認）。
- [x] **R11 料金ページで、セミナー価格・モニター価格の契約中に「現在のプラン」が出ない。**
      `shared/plans.ts` に `currentPlanCardId` / `campaignPriceLabel` を足し、Pricing.tsx はそれを使う。
      pro_seminar の方には**プロプランのカードに「現在のプラン（セミナー価格）」**と出て、ボタンも押せなくなる。
      ローカルQAで pro_seminar の契約を作って375pxで実画面を確認（横はみ出し・JSエラーなし）。
      テスト `server/pricingCurrentPlan.test.ts`（6件・全キャンペーンプランがカードのある通常プランへ向くことも固定）。

- [x] **R12 毎朝6時の承認カードに「どのアカウントの投稿か」が出ない。**（2026-09-16 日中に反映・三上様「すぐに修正して」）
      `server/lineChat.ts:93` のカードは先頭に `@アカウント名　MM/DD HH:MM 公開予定` を出す作りだが、
      `accountName` は任意。**朝6時の経路（`server/autoPostScheduler.ts:1332`）が渡していない**ため時刻だけになる。
      LINEメニュー「今日の投稿」（`lineChatHandler.ts:262` の `withAccountNames`）と、見送り／書き直し後の返事には出るので、
      同じ投稿なのに出たり出なかったりする。→ 朝6時の `posts.map` に accountName を足す。
      **あわせて `awaitingSlideJob`（18:00）と `morningDigestJob` 経由のカードも同じ経路か点検する。**
      2026-09-16 梅原様が接骨院の分とダイエットの分を取り違えかけた（複数アカウント運用の実害）。
- [x] **R13 複数アカウントの方は、カードのアカウント名を見出しとして目立たせる。**（同上）
      いまは `size:"xs"` の薄いティール1行目で、本文に埋もれる。2アカウント以上の方だけ、
      太字・大きめ・本文と区切る（1アカウントの方は従来どおりで変えない）。

### ★最優先（2026-09-13 三上様「一日8件投稿やったらあかんやろ」）— 上の R1〜R8 に統合。以下は経緯の記録として残す
- [x] **1日の上限を全アカウントに掛ける。** いまは公開時の上限チェック（`server/scheduledPostExecutor.ts` の ramp cap）が
      `rc.capped` のとき＝慣らし運転中か冷却中のアカウントにしか効かない。Threads歴の長いアカウントには上限が無く、
      9/12 に Moveact 10件・しっとる公式 8件・滝本様 8件・岩根様 8件が公開された（全体65件／平常38件前後）。
      契約本数＋補填分を上限とし、ご本人の手動投稿も数えて超えた分は公開せず見送る（翌日へ）。
- [x] **前日の未確認カードが翌日に残り、当日分と合わせて契約本数を超える枚数が同じ日に届く。** 岩根様 9/12：
      9/11 の未確認4件＋9/12 の5件＝9件のカードが同日に届き、ご本人が夕方に8件を承認（追い投稿ゼロ・自動公開は未反映・
      8件ともご本人の操作）。生成時に「その日の予定＋残っている確認待ち」で本数を見る。
- [x] **消された投稿の補填が自動では積まれない。** 品質で落ちた枠（`shortfallCount`）は補填されるが、
      公開後に Threads 側で消された分は記録されない。`accountHealthJob` が検知した消失件数を補填キューに積み、
      **冷却明けから1日＋1件ずつ**返す。髙木様（@miraiseitai.diet・3件消失・9/20まで冷却）に文面で約束する内容。
- [x] **追い投稿は承認制でない方に承認なしで出ていた。** 滝本様（承認制なし・契約3件）は 9/4〜9/12 の8日間、
      毎日4〜8件（うち追い投稿1〜4件）。ご本人は3件のつもりで、実際はその倍。岩根様は 9/12 に8件（追い投稿0・繰り越し二重）。
      お客様への説明文は三上様承諾のうえ送付（文面は 9/13 朝の点検チャットに用意）。今後「契約本数の外で出るもの」は作らない。
- [x] **承認の記録が残っていない。** 「これで投稿する」「すべて承認」「見送りなし公開（自動）」のどれで公開に至ったかを
      `scheduledPosts` に残す（approvedAt / approvedVia）。9/13 三上様「この人は本当に前日に承認していますか？」に、
      editedByUserAt と公開時刻から推定するしかなかった（岩根様 9/12 の8件中2件は本人の押下か自動かを区別できない）。
- [x] 承認待ちが日をまたいで残っているとき、翌日の新規分と合わせた「その日の確認カード」が契約本数を超えないようにする
      （岩根様 9/12：前日分4件＋当日分5件＝9件のカードが同じ日に届き、夕方に8件承認→その晩にまとめて公開）
- [x] 冷却に入った日は、その日すでに作ってある予定も1日1件まで絞る（今朝 acct16 は3件作られた。
      公開時の上限で2件はキャンセルされる見込みだが、承認カードだけ届いて公開されないのは分かりにくい）

- [ ] `naturalnessReview` が短い定型（「土浦で11年。」）に3/5を付け続ける。採点基準と生成プロンプトの整合を見る
      （9/12の24時間：2/5が11件・3/5が37件・4/5が11件・5/5が61件。3/5で落とすのはやめてあるので投稿は出ている）
- [ ] 文体のお手本（`styleSamples`）が空のまま3日以上の契約者が11名
      （しっとる広告・三上様・滝本様・髙木様・小林様・プレステージ様・小西様・比嘉優様・佐々木様・juria様・大木様）。
      毎朝の案内は出ている
- [ ] 手順書 `docs/runbook-quality-shortfall.md` と `scripts/ops/fill-shortfall.mts` を朝の点検 §2.7 で運用
- [ ] 代理店：9/15（月）9:05 に `[AgencyReport] 代理店契約なし` が出る／9:30 の社内報告に「公式LINEの月間枠」
- [ ] 資料台帳・Notionの代理店紹介資料の差し替え（ts-deck の pptx/pdf は再生成ずみ）

---

## 2026-09-14 未明の通し確認で見つけて直した分

ローカルQA（port 3100）で、新しいお客様の目線でひととおり操作した結果。

- [x] **「はじめの設定」を終えた直後に「固定投稿を作る」を押すと、たった今やり終えた設定を
      もう一度やらされる形になっていた**。固定投稿は業種・場所・お客さん像・お悩み・強みの5つを
      すべて要る作りなのに、「まず5つ」で埋まるのは業種・場所・お悩みの3つだけ。
      そのため「まだ『お店の情報』が登録されていないため、投稿を作れません。先に『はじめの設定』を
      お願いします」と返っていた（お店の情報は登録されているので、事実とも食い違う）。
      足りないのが「お客さん像」「強み」だけのときは、そう言って**その2問へ直接ご案内する**ようにした
      （`pinnedPostFlow.ts`・`server/pinnedNeedsMore.test.ts` 5件）。
      ※必須条件そのものを減らすかは「判断待ち D」のまま。ここでは案内だけを直した。
- [x] **「まず5つ」の4問目が「そのお客さんは、どんなことで困っていますか？」だった**。
      お客さん像（targetRaw）は「まず5つ」では聞かないので、「その」が何を指すのか分からない。
      「お店に来るお客さんは、…」に直した（説明書 threads-setup-manual.html の書き方と一致）。
      `server/quickQuestions.test.ts` で、聞いていない質問を指す言い方が入らないように固定。
- [x] **「ありがとうございます」に「ご用件を下から選んでください」と返していた**。
      お礼・あいさつだけの短い一言は、AIを呼ばないまま短くお返しする（`isThanksOrGreeting`）。
- [x] **ひらがなの「つかいかた」がどこにも当たらず、受け皿に落ちていた**（「使い方」は通る）。
- [x] **承認の記録が画面に出ていなかった**（R7の残り）。投稿の履歴に
      「承認: 2026/09/14 01:26 LINE（1件ずつ）」のように出す（`PostHistory.tsx`）。
      line_one / line_all / auto_soft / web の4通りを実画面で確認。記録の無い古い投稿には出さない。

### 髙木様（account 16）の補填は `deletedShortfall` に積まない（2026-09-14 に確認）

R6 の宿題に「冷却 8日×2件＋消失3件＝19件」と書いてあったが、**実際には二重になる**ので積まない。
理由：`accountHealthJob` が消えた投稿を `status='failed'` に落とすため、
`countAccountAutoPostsSinceConnect`（`status IN ('posted','pending','awaiting_approval','processing')`）から外れる。
つまり消えた3件も冷却で減った分も、**連携30日以内は `compensationCount` がまとめて不足として拾い、1日＋1件で返す**。
account 16 は連携 2026-09-01（30日以内は 10/1 まで）なので、9/13 にお約束した
「減った分と消えた3件を1日1件ずつ足す」は今の仕組みのままで果たされる。
`deletedShortfall` は 0 のままが正しい。**手で入れないこと。**
（30日を超えたアカウントで消失が起きたときだけ `deletedShortfall` が効く＝R6 の本来の役目）

### 確認できたこと（問題なし・2026-09-14）
- 会員登録 → ログイン → 料金プラン → 「このまま無料で始める」／「7日間無料で試す」→ 決済ページ（checkout.gopay.jp）まで、
  **ポップアップを遮断した状態でも同じタブで進める**。
- 375px で主要22ページに、ページ全体の横スクロール・JSエラー・空表示なし。
- LINEのボタン47種すべて応答あり（無反応・「うまく受け取れませんでした」ゼロ）。自由入力12種も的外れなし。
- 「はじめの設定」は5問を最後まで通せ、確認画面 →「この内容で登録する」→ 次にやること まで正常。
- `npx tsc --noEmit` エラー0／`npx vitest run` 782件通過・失敗0（9/13 の769件＋今夜の13件）。

### QAスクリプトの直し（誤判定をなくした）
- 375px のはみ出し判定を、要素ごとではなく**ページ全体が横スクロールするか**で見るようにした。
  比較表のように `overflow-x:auto` の中で伸びているものは、はみ出しではない。
- 会員登録の自動操作が、同意チェック（shadcn は `button[role="checkbox"]`。`input` は隠れている）を
  押せておらず、**アプリ側の不具合のように見えていた**。実際には登録は通る。

---

## 2026-09-14 追加（Keiro の決済通知が未接続＝最優先・期限あり）

**背景**：UnivaPay の通知の送り先に `keiro.s-toru.com` が登録されておらず、Keiro は一度も決済通知を
受け取っていない（`payments` 0件）。そのため **初の外部有料契約（洋菓子の店モンテローザ様・
岡田祐一様 `monteroza966@gmail.com`・プロ月9,800円・初回課金 2026-10-13）が Keiro の DB に存在しない**。
モンテローザ様の `trial_ends_at` も 2026-10-13 なので、**このままだと課金と同じ日に無料期間が切れて計測が止まる**。
たきもと様は 2026-10-08 に無料期間満了。

**コードは反映待ち**（`shittoruad-gif/keiro` main の `a506b77` と `ec6c527`。selftest 92件PASS）。
`keiro-shittoru` は自動反映の対象外なので、**手動デプロイが要る**。

- [ ] **1. 控えを取る**：`docker exec x10e9syw5oydt9pqw6hqwiij-… node scripts/backup.js`
- [ ] **2. keiro-shittoru を手動デプロイ**（Coolify uuid `x10e9syw5oydt9pqw6hqwiij`）。
      ⚠️ 同じリポジトリから `keiro`（uuid `fstao1mijd4hgpu67j7yx7xv` = keiro.moveact.net）も動いている。
      **今夜は keiro-shittoru だけを反映する**（Moveact 側は金曜の会議の範囲）。
      反映後：`https://keiro.s-toru.com/` が 200／ログに `決済リンクIDを解決しました count=5` が出ること。
- [ ] **3. 通知の送り先を登録**：`docker exec <keiro-shittoru> node scripts/ensure-webhook.js` で
      「未登録」を確認 → `node scripts/ensure-webhook.js --apply` で登録 → 一覧が3本になることを確認。
- [ ] **4. 取りこぼしを拾う**：`node -e` で `reconcile.adoptOrphanSubscriptions(db)` を1回流す
      （日次でも走るが、今夜のうちに確定させる）。
      **拾うのはモンテローザ様1件だけが正しい**（本番で予行演習済み・DBは未変更）。
      でみず様の交通事故4件（16,500円・660,000円×3）は「除外・他事業」になること、
      でみず様のテナントが `suspended` のままであることを必ず確認する。
- [ ] **5. 確認**：Keiro の `subscriptions` にモンテローザ様の行（status=active・初回請求 2026-10-13）が入り、
      運営画面で「ご契約あり」になること。

**なぜ登録だけでは駄目だったか（先に直した理由）**
- UnivaPay のストアは全事業で共用（Threads Studio・交通事故・Instagram広告・Keiro）。
  通知の送り先はストア単位でしか設定できないので、登録すると **125件すべての決済通知が Keiro にも届く**。
  直す前の実装は「知らないメールアドレスの決済」ごとに運営へメールを出すため、**月末（9/30）に100通を超える**。
- さらに、**メール一致だけでは商品を見分けられない**。でみず鍼灸整骨院の出水様は Keiro のテナントかつ
  交通事故のお客様で、交通事故の入金で停止中のテナントが復活してしまう。
  → 決済リンクID（`UNIVAPAY_LINK_URL_*` の5本）で見分けるようにした。

**残る判断（三上様）**：たきもと鍼灸整骨院様の枠は `shittoru.ad+takimoto@gmail.com`（しっとるのメール）で
作られているため、滝本様ご本人のカードでは紐づかない。「しっとるが持つ」のか「院にご契約いただく」のか。
無料期間は 2026-10-08 まで。

---

## ~~★9/15 三上様指示：健康表現は「最初から書かない」~~ → **2026-09-16 未明に反映ずみ**

3点とも入れた。詳しくは下の「2026-09-16 未明に直した分」と `docs/safe-operation-rules.md` の追記へ。

---

## ★9/15 三上様承諾：慣らし運転の説明を全体お知らせに入れる

小西様（5002）への訂正連絡を 9/15 に送付した際に判明したこと：
**慣らし運転（新規アカウントは10日目まで本数を抑える）の説明が、全体お知らせに一度も入っていない。**
9/9・9/11・9/13 の3通はいずれも別件。個別に触れたのは 9/11 の訂正文の1文だけだった。

**2026-09-16 未明：文面を用意しました。まだコードに入れていない＝送られません。**
`scripts/ops/announcements/2026-09-16_ramp_notice_draft.txt`（送る相手・送らない相手も書いてあります）。
三上様の承諾をいただいた夜に `shared/announcements.ts` へ足し、`sendOn` をその翌日にします。
※ 2026-09-07 にも同じ趣旨の文面（`2026-09-07_ramp_notice.txt`）が用意されていましたが、
　 `DAILY_ANNOUNCEMENTS` に入っておらず**一度も送られていません**でした。小西様のご質問（#33）の元はここです。

夜間整備でやること:
1. `shared/announcements.ts` に慣らし運転のお知らせを1本追加する。
   入れる内容 = ①なぜ抑えるのか（Meta側の自動判定でアカウントが止まるのを防ぐ）
   ②日数と本数（1〜5日目1件・6〜10日目2件・11日目から契約どおり）
   ③抑えた分は1日＋1件で取り戻し、30日間で契約どおりの合計になる
   ④お客様がすることは無い
2. `when` は「連携30日以内のアカウントをお持ちの方」に絞る（全員に出すと関係ない方が混乱する）。
3. **sendOn は本番反映の翌日以降にする（R4）。反映をデプロイ記録で確かめてから日付を決める。**
4. 送信前に三上様の承諾を得る（お客様に届く送信のため）。

対象になりそうな方（9/15時点で連携30日以内）：プレステージ様（22）・小西様（24・送付済み）・
髙木様（15/16）・氷見様（17）・小林様（19）・香取様（21）・岩根様（25）・haisaiseikotsuin（26）ほか。
