# 夜間整備への申し送り

朝の点検（`threads-studio-daily-check`）で見つけたが、日中は本番に反映しないため夜へ回したもの。
片づいた項目は消してよい。

---

## 三上様の判断待ち（送信していません）

### A. 小林武晴様の「強み・実績」の言い換え

- 9/8に `healthClaimGuard` が同日2件発動（`治る・改善の断定` を落とした userId=3200）。
  安全運用ルールの「同じアカウントで1日2件以上なら、はじめの設定の『強み・実績』に結果表現が多い」に該当。
- 文面は用意ずみ。**三上様の承諾をいただいてから**お送りします。

### B. 佐々木竜也様（9/9ご登録・pro）の登録内容が業種と食い違う

- 業種「コンサルタント」に対し、お客さん像に「デスクワークの会社員」、強みに「専門特化（〇〇専門）」と
  治療院の言葉が入っている。9/11の朝の点検にも同じズレが出ている。
- 業種ズレの重複通知は止まっている（`industryMismatchNoticeKey`）。
- 文面は用意ずみ。**三上様の承諾をいただいてから**お送りします。

### C. Coolifyの環境変数 `RESEND_FROM_DOMAIN=resend.dev` を消す（運営作業）

- 本番の実行中コンテナは `threads-studio.com` を正しく読めている。重複しているのは
  **プレビュー用（`is_preview: true`）の1件**（uuid `nioepcaeeyhgczsx70vyhydx`）。
- 9/9 01:19 のデプロイ中に実際にこちらが拾われ、お客様宛メールが1通落ちている。
  環境変数の削除は本番設定の変更なので夜間整備では触っていない。

### D. 自動投稿の必須条件を減らすか（2026-09-11）

- 「はじめの設定」は5問で終わるのに、自動投稿は今も **お客さん像（target）と強み（strength）** を必須にしている
  （`autoPostScheduler` の `eligibleProjects`）。9/11未明の時点で大木様・juria様（どちらもプロ・9/9ご登録）が
  強みだけ空のまま投稿ゼロだった。
- 9/11の夜間整備では**案内だけ**を直した（足りない項目を名指しして、その1問をその場で聞く＝`project_almost`）。
- 必須条件そのものを「業種・地域・お悩み」まで減らすと5問だけで投稿が始まるが、強みが無いぶん投稿は弱くなる。**要ご判断。**

---

## 恒常のメモ

- LINEの月間通数はコミュニケーションプラン（5,000通）。朝の点検に残量が出る（残り1割で警告）。
  通数切れのときは、お客様へは同じ内容がメールで届き、運営にも1日1回知らせが飛ぶ。
- ローカルQAは port 3100（`QA_SAFE_MODE=1`）。DBはローカルMySQLなので本番には触れない。
  `handlePostback` / `handleFreeText` を直接叩く確認は、`BUILT_IN_FORGE_API_KEY` と `BUILT_IN_FORGE_API_URL`
  を Coolify の env から渡さないと自動応答が動かない（`.env` には入っていない）。

---

## 2026-09-11 未明にデプロイした分（本番反映済み・翌朝に見ること）

デプロイ 01:38 → 01:46 切り替え完了（`4557b15`）。9/10朝から溜まっていた未pushの21コミットを含む。

- [ ] 6時の生成ログ `docker logs | grep -c "identityGuard: この店を指す言葉が無い"` が **19件から減っているか**
      （9/10朝の失敗の最大要因。プロンプトと検査の矛盾を直した分がここで効く）
- [ ] `naturalnessReview` の 2/5 が減っているか。特に岩根様（account 25）に3件届いたか
      （9/10は3枠すべてが3回とも 2/5 で投稿ゼロ）
- [ ] `voiceGuard: 敬語の登録なのに砕けた締め` が氷見様（17）・プレステージ様（22）で出ていないか
- [ ] 7:40 `[MorningDigest] 送信 N件`。8:30／8:35 に別の通知が出ていないこと
- [ ] 7:40 の直後、個別のお知らせ（お詫び）6通が届いたか（`shared/personalNotices.ts`）
- [ ] 大木様・juria様の朝の案内が「あと1問だけ答える」になっているか（`project_almost`）
- [ ] 9/12 6時に `自動補填: 昨日届かなかった…` が出るか
- [ ] 10:00 の Meta AI呼びかけで `送信停止` が出て、対象の方に「再開する」ボタン付きの一言が1回だけ届くか

## 2026-09-11 朝（三上様「すぐに対処」：香取様の今朝0件）

- 9/11 6時：35枠中19枠が失敗。3回目の理由の半分以上が naturalnessReview 3/5。香取様は2枠とも0件 → 個別通知が条件で見送られた。
- 対処（ローカルの修正版で本番DBに対して実行）：最後の作り直しは3/5を通す（コミット 510bfe5・今夜デプロイ）。香取様の2件を当日補充で作成（#1297・#1298）→ お詫び文を手で送信（08:5x）。他11アカウントの不足分も当日補充を実行（結果は fill_all.log）。
- 気づき：
  - [ ] 当日補充が「直近30分の承認待ち」をまとめて再送するため、続けて実行すると同じカードが2回届く（香取様に3回届いた）。補充で作った分だけ送るように直す
  - [ ] `fillStyleSamplesForAccount` が香取様の2025年の古い投稿を選び、直近の質の高い投稿（「患者さんからよく聞く言葉→コメント欄👇」型）を取っていない。選び方（新しい順・返信除外・長さ）を見直す
  - [ ] naturalnessReview が短い定型（「土浦で11年。」）に3/5を付け続ける。採点基準と生成プロンプトの整合を見る（週次リサーチの「決まり文句の流出はプロンプトの自己矛盾」と同根）
  - [ ] 固定投稿の下書き（source=manual, angle=pinned）が承認待ちのまま approval_reminder で毎日翌日へスライドし続けている（廿日市様：9/4から3件）。固定投稿の下書きは承認リマインドの対象から外す／7日で取り下げる。毎日メールが届いている可能性があるので確認
  - [ ] 手順書 docs/runbook-quality-shortfall.md と scripts/ops/fill-shortfall.mts を朝の点検 §2.7 で運用開始（9/12朝から）

## 2026-09-11 昼（三上様：通常投稿に固定投稿のようなものを混ぜない）

- 誘導文の付け足し停止・reservation_funnel 除外・quote_boost 停止・固定投稿下書きの分離（コミット済み）。本番DBでは 1236（よくなる様の引用投稿）と 915・1023〜1025（固定投稿の下書き）を取り下げ済み。
- デプロイ後に確認：9/12 6時の生成に reservation_funnel が無い・本文末尾に「ご相談は…から」が付いていない・水曜(9/16)に quote_boost が動かない。

## 2026-09-11 昼（三上様：承認が間に合わない先生・コメント即時）

- awaiting_slide（30分おき 7:00〜21:30 JST）・evening_approval_reminder（18:00）・comment_watch 15分おき（7:00〜21:45）・遅い承認は翌朝10時台（shared/publishTiming.ts）。コミット済み、今夜反映。
- ✅ Threads Webhook 登録済み（2026-09-11 11:00ごろ・三上様のChromeで実施）：Meta App「Threads Studio」(1250891946948510) → ユースケース Threads API → 「ThreadsでWebhook」を追加 → Moderate を Subscribe（callback https://threads-studio.com/api/threads/webhook・検証OK・fields: replies / delete v1.0）。ダッシュボードに Test ボタンは無い。最初の実イベントは `docker logs | grep ThreadsWebhook` で確認。comment_watch（15分）は保険として残す。
- デプロイ後に確認：`[AwaitingSlide]` が30分ごとに出る／18:00に `[EveningReminder] 送信` ／comment_watch が15分ごと（API 429 が出ないこと）。
- 承認待ちのまま公開ゼロの方への案内（朝のまとめ・18時）に「承認がないと投稿されない」と「自動にする（確認なし）」ボタン（d705277）。デプロイ後：該当者が出た朝に `★昨日は、承認待ちのまま` が本文に入っているか確認。

## 2026-09-11 昼（三上様「すべてその通りに」：代理店プラン）

- コミット済み（今夜反映）：APIアクセス表記の削除／公式LINE月間枠の監視（9:30 社内報告に残量、7割で運営へ警告）／代理店向け週次まとめ `agency_weekly_report`（月曜 9:05 JST・server/agencyReportJob.ts）／配下クライアントの制限・投稿消失を代理店へ即時連絡／ログイン前トップ（Landing.tsx）の料金欄を実態に合わせる／代理店クライアントの料金ページに「代理店契約に含まれています」。
- 通し確認（ローカルQA・QA_SAFE_MODE=1）：代理店テストID→クライアント発行→クライアントでログイン→LINE友だち追加の1本道→Threads連携 0/1 まで正常。クライアント管理メニューはクライアントに出ない。
- 資料：LP（threads-studio-lp）に代理店プランを追記して公開済み。カタログ（service-catalog）は services.json に代理店を足して build-all 済み（デプロイは週次同期タスク）。代理店紹介資料（ts-deck/ThreadsStudio_代理店プランのご案内.pptx/.pdf）はFAQに「白ラベルではない」「週次まとめ」を足して再生成済み（資料台帳・Notionの差し替えは未）。
- デプロイ後に確認：9/15（月）9:05 に `[AgencyReport] 代理店契約なし` が出る（契約が入るまで送信なし）／9:30 の社内報告に「公式LINEの月間枠」が出る。

## 宿題（規模が増える前に）
- [ ] 朝6時の生成を並列化（ユーザー単位で4〜6並列・LLMのレート上限を確認）。9/11実測：40枠で75分。連携50アカウントで4.7時間になる（docs/scaling-thresholds.md）。

