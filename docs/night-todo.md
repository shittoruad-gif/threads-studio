# 夜間整備への申し送り

朝の点検（`threads-studio-daily-check`）で見つけたが、日中は本番に反映しないため夜へ回したもの。
片づいた項目は消してよい。

> 2026-09-19 の夜間整備で、片づいた記録（9/13〜9/18 の作業ログ）をこのファイルから外した。
> 過去の経緯は `git log docs/night-todo.md` から追える。ここには **これからやること** と
> **忘れると事故になること** だけを残す。

## 2026-09-22 未明（夜間整備）にやったこと

### 1. 作り直しの理由をDBに残すようにした（新規・0093）

**きっかけ**：この手順書の §3.45 は「その日に作り直しで落ちた理由を
`docker logs | grep` で数える」ことになっているが、**今夜は1件も数えられなかった**。
本番のコンテナは 9/21 09:52 の再デプロイで作り直されており、6時の生成のログが残っていない。
このファイルの「忘れると事故になること」に書いてあるとおりの事態
（＝**数えたい検査は、ログではなくDBに印を残す形にする**）なので、そのとおりにした。

- `postRejectLog` テーブル（マイグレーション `0093_post_reject_log.sql`）。
  検査の名前（guard）・落ちた中身（detail）・**その枠を捨てたか（gaveUp）** を残す
- `server/autoPostScheduler.ts`：`lastRejectReason.set(...)` を直に呼んでいた**9か所**を
  `noteReject(...)` に置き換えた。次の作り直しへ理由を渡す動きはこれまでと同じで、同時にDBへ残す。
  **記録に失敗しても生成は止めない**（await しない・例外を握りつぶす）
- `scripts/ops/reject-reasons.mjs`（新規）：夜間整備で数えるためのスクリプト。
  理由ごと／アカウントごとの内訳と、**同じ理由で3回以上「枠を捨てた」アカウント**を出す
- `server/postRejectLog.test.ts`（新規・6件）：`lastRejectReason.set` を直に呼ぶ書き方に戻ったら落ちる番人

**明日の夜からの使い方**（§3.45 の「ログで数える」の代わりに、こちらを使う）:

    nc -z localhost 13308 || ssh -fN -L 13308:10.0.1.7:3306 root@163.44.103.9
    eval "$(bash scripts/ops/prod-env.sh DATABASE_URL)"
    node scripts/ops/reject-reasons.mjs 24

※ 記録は 9/22 の反映以降の分だけ。それ以前は残っていない。

### 2. フォロワー数の日次スナップショット（9/21 日中のコミットを今夜 push）

### 何が起きていたか
`followerSnapshots` テーブルは最初から在るのに、**1件も貯まっていなかった**。
原因は `server/threadsApi.ts` の `getThreadsUserCounts` が
`GET /v1.0/{user-id}?fields=followers_count,following_count` を叩いていたこと。
このフィールドはユーザーノードに存在せず、実測で必ずこう返る：

    {"error":{"message":"Tried accessing nonexisting field (followers_count)","code":100}}

そのため常に0となり、`dailyOpsJobs.snapshotFollowersForUser` の
`if (followers <= 0) continue;`（0しか取れないアカウントは記録しない）に毎回かかっていた。
毎朝7:00のジョブ自体は動いていたが、書くものが無かった、という状態。

### 直した内容
- `server/threadsApi.ts`：取り口を `GET /v1.0/{user-id}/threads_insights?metric=followers_count` に変更
  （`server/accountRampCheck.ts` が前から使っている、実際に動く方）。
  戻り値に `ok`（APIから値を取れたか）を足し、**取れての0人**と**取れなくての0**を区別できるようにした。
  `following_count` は Threads API に相当する指標が無いため 0 のまま。
- `server/dailyOpsJobs.ts`：`counts.ok` で分岐。取得に失敗したときだけ連携時の保存値へフォールバックし、
  本当に0人のアカウント（開設直後）は**0として記録する**ようにした。
- `server/threadsFollowerCount.test.ts`（新規・7件）：`fields=followers_count` へ戻ったら落ちる番人。
- `scripts/ops/snapshot-followers.mts`（新規）：手で当日分を取る運営用スクリプト。`--dry` で取得のみ。

### 反映を待たずに済ませたこと
9/21 13時台に `snapshot-followers.mts` を本番DBへ実行し、**22アカウント全件の当日分を記録ずみ**
（合計フォロワー 3,014・0人が4アカウント）。書いたのは `followerSnapshots` だけで、
お客様への送信・設定変更は一切していない。

### 今夜やったこと / 明朝みること
1. ~~push~~ → **9/22 未明に push ずみ**（auto-deploy が拾う）
2. ~~反映の確認~~ → **本番のイメージタグが一致することを確認ずみ**
3. **明朝7:00の `analytics_snapshot` の後**、`SELECT capturedOn, COUNT(*) FROM followerSnapshots GROUP BY 1`
   に **9/22 の行が22件前後で増えている**ことを確認する。増えていなければジョブ側を疑う（← これだけ残り）

### なぜやったか（2026-09-21 三上様）
「導入前から動かしていた店」と「導入後から動かし始めた店」の違いを出したとき、
1日あたり表示に 591 対 376 の差が出たが、**フォロワーの記録が無いため原因を説明できなかった**。
30日ぶん貯まれば、この差がフォロワー資産によるものか投稿の中身によるものかを判定できる。

---

## 2026-09-21 未明（夜間整備）に直した分

### 1. 承認待ちの投稿を翌朝へずらしていたため、公開が0件の日ができていた

- **香取様（acc21・light_campaign・公開前確認あり・1日1件）は、直近7日で公開1件・見送り6件。**
  9/15・9/16・9/17・9/19・9/20 は**投稿が1本も作られていなかった**（scheduledPosts の createdAt で実測）。
- 仕組み：承認待ちのまま時刻を過ぎた投稿は `awaitingSlideJob` が後ろへずらす。19時以降は「明日の10時台」へ送っていた。
  ところが翌日は「承認されないまま日をまたいだ」として `promoteSoftApprovedDuePosts` が**必ず見送りにする**（R2・9/12の決まり）。
  さらに朝6時の生成が `countAccountAutoPostsScheduledToday` で「今日すでに1件ある」と数えるため、**新規を1本も作らない**。
  結果、その1件も昼に消え、その日の公開は0件。翌日また同じことが起きる。
- 直した：19時以降はずらさない（`slideOverdueTime` が null を返す）。そのまま置けば日付が変わった直後に見送りになり、
  朝6時には新しい投稿が作られる。遅れて承認された場合は `lateApprovalTime` が今までどおり拾う（取りこぼさない）。
  あわせて、見送りが決まっている承認待ち（前日以前に作られたもの）は1日の枠にも数えないようにした。
  テスト `server/awaitingSlideNoCarry.test.ts`・`server/publishTiming.test.ts`。ルール文書 `docs/safe-operation-rules.md` の2番に追記。
- **明朝みること**：香取様（acc21）に 9/21 の投稿が作られているか。作られていれば直っている。

### 2. 登録直後の画面で「コピー」が画面の外にあって押せなかった（375px・390px）

- 新規のお客様が登録後に必ず通る画面（`OnboardingStart`。「公式LINEを友だち追加してください」）の
  手順2で、6桁の連携コードの横の「コピー」が**右へ20px はみ出して押せない**状態だった。
  数字（`tracking-[0.3em]`＋`min-w-[9rem]`）とボタンの合計が、箇条書きの中の幅を超えていたため。
  320px・375px・390px のいずれでも起きていた（数字自体は見えるので、手で打てば進めた）。
- 直した：折返せるようにし、数字の字間はスマホだけ詰める。320/375/390/768px で確認し、
  「コピー」「公式LINEを友だち追加」の両方が押せる（何にも覆われていない）ことを実測。

### 通し確認でわかったこと（ローカルQA 3100・本番には触れていません）

- 会員登録 → ログイン → 料金プラン → **有料4プランすべて**、ポップアップを遮断した状態（`--block-new-web-contents`）で
  同じタブで決済ページ（checkout.gopay.jp）まで進める。JSエラーなし
- 375px の主要37ページ：横スクロール・空表示・JSエラーなし。指摘は上の `/dashboard` 1件だけ
  （`/pricing` の機能比較表のはみ出しは `overflow-x-auto` の中＝横に送って読む作りなので問題なし）
- LINEのボタン・自由入力 83種すべて応答あり（無反応・THROW・「うまく受け取れませんでした」はゼロ）
- 「はじめの設定」は 1／20 → 8／20 まで通し、前回の答えの差し込み・「戻る」・文章での回答が効くことを確認
- 固定投稿：作る → 品質検査（healthClaimGuard が「原因の断定」を落とした）→ 400字ほどの案 →
  ピン留めの手順案内 → 「ピン留めしました」の記録まで通る
- `npx tsc --noEmit` エラー0／`npx vitest run` は **117ファイル・1051件成功（失敗0・16件スキップ）**

### 直していない（確かめたうえで「仕様どおり」と判断したもの）

- 運営確認待ち（`adminReviewRequired=1 AND adminReviewAt IS NULL` かつ pending/awaiting_approval）は**0件**
- 朝7:40のご案内が届いていない方（`nextActionLastSentAt` が NULL／古い）は、いずれも
  **次にやることが無い方**だった（小林様・氷見様・香取様）。9/19に直した斎藤様には 9/20 07:40 に `no_account` で届いている

## 2026-09-19 日中に片づけた分（★9/19 20時ごろに反映ずみ。本番は cde5899）

### 有料のお客様が「お試し」扱いで朝の案内から外れていた（真因・c827f40）

- 斎藤様（10043・pro_campaign・9/16課金済み）は isDemoMode=1 のままで、morningDigestJob が黙って対象外にしていた。
  「次にやること（Threadsを連携してください）」が**3日間1通も届かず**、Threads未連携・投稿0で止まっていた。
  お店の情報（業種・地域・お客さん像・悩み・強み・実績）もLINE連携も 9/16 に完了しており、足りないのは連携だけ。
- isDemoMode はデモの生成枠（10件）を使い切ったときにしか下りない作り。**ご契約しても下りない。**
- 直した：`createSubscription` / `updateSubscription` が active・trialing になったらお試し扱いを解除。
  `morningDigestJob` は isDemoMode でも「お支払いのある方」は必ず案内する（その場で記録も直す）。
  回帰テスト `server/demoModePaid.test.ts`。
- **本番データは日中に直した**（明朝7:40を確実に届けるため）：しっとる広告(1)・大木慎也様(7961)・斎藤様(10043) の
  isDemoMode を 0 に。契約の無いお試しの方（岸本様・久米様・宮本様・Fhry）は**触っていない**。
- 斎藤様には 11:0x に連携のご案内をLINEで送信済み（三上様の明示承諾あり・「アカウント連携」ボタン付き）。
  **明朝の点検で、連携されたか・7:40のご案内が届いたかを必ず確認する。**

### 冷却の見直し（eb6860b）と、コメントの「返信しない」（5a6bdf5）

- 詳細は上の A1・A4 と、`docs/commitments-2026-09.md` の 9/19 朝の点検メモ。
- ★お客様に届く文面（`singleDeleteAskNotice` ほか）は、**三上様の承諾をいただいてから反映する**。

### 朝の報告の訂正（2026-09-19・2件）

- 「冷却中の香取様が1日2件になっている」は**投稿を作る段階の話**だった。公開直前に `dailyCapCheck` が
  もう一度上限を見るため、2件目は翌日へ送られる。**実際に公開されるのは1日1件のまま**で、アカウントの危険は無かった。
  三上様には訂正済み。繰り越しの直し（eb6860b）は、作る段階でも数を合わせるためのもの。
  **裏取り（9/19 昼）**：冷却中のアカウントの実際の公開数を10日分数えたところ、
  acc16 は 9/13〜9/15・9/18 が各1件、acc29 は 9/18 に1件、acc14 は 9/13〜9/16 が各1件で、
  **冷却中に1日2件公開された日は無い**。公開直前の上限（dailyCapCheck）は実データでも効いている。

- 「契約中なのに投稿ゼロ＝斎藤様・大木様・juria様の3名」も不正確だった。
  大木様・juria様は**お支払いのない期限なしトライアル**（PROST2026・三上様が放置と決めた5件）。
  正しくは「**お支払いのある方で投稿ゼロ＝斎藤様1名**」。

---

## 香取様の投稿ゼロ：真因と直し（2026-09-21 日中・三上様の指示で反映ずみ）

**訂正が2つあります。**

1. 朝に「原因は繰り越し」と書きましたが、**9/21 のゼロは別の原因**でした（繰り越しの直しは 9/20 までの分には効いています）。
2. 朝に「材料が2つしかないのが根っこ」と書いたのは**誤り**です。香取様のご登録内容は十分ありました。

### 何が起きていたか（本番ログとDBで確認）

直近14日で公開できたのは1本。見送り11件の内訳は
**お客様ご自身が「違う」と押されたもの8件**・日をまたいで見送り2件・繰り越しの不具合1件。
ほかに Threads側で削除2件（→9/25まで冷却）。

お客様が✕にされた8本は、すべて「整形外科で**11年**」＋「痛む場所だけ触っても」でした。
9/21 も同じ型に戻り、4回とも差し戻されて公開ゼロ。

### 真因：信条（belief）と実績（proof）だけ日替わりで回していなかった

    信条  「痛い場所をマッサージするだけでは良くなりません。昔はマッサージばかりやっていた。」
    実績  「整形外科で11年勤務／学会で11年連続で発表」

悩み・強み・N1顧客像は `pickRotatingTopic` で日替わりに回しているのに、この2つは回さず、
さらに信条には「**投稿に一貫してにじませる。仮想敵型と相性が良い**」という強い指示が付いていた。
だから毎回この2つが本文に出て、重複ガードに弾かれ続けていた。

**一方で、一度も使われていなかった材料がありました**：
小学生が捻挫かと思ったら骨折→エコーで確認→サッカー復帰／50代男性の腰痛／大会前の中学生／
夜21時まで営業／外傷専門特化。

### 直したもの（c49c197・9/21 09:47 反映。ガードは緩めていない）

- 信条・実績も複数行なら日替わりで1つにする
- **直近10本ですでに使われている言い回しは、その日は渡さない**（渡さなければ別の材料から書くしかなくなる）
- 信条の指示を「一貫してにじませる」→「考え方の土台。文言をそのまま書かない」へ
- 保証パスが一般論（「寝る前5分のワザ」等）に逃げないよう、実際にあった一件をその場面のまま書かせる

香取様の実データで、直す前と直したあとを4回ずつ生成して確認。直したあとは毎回、
未使用のN1（小学生の骨折→サッカー復帰）と強み（外傷専門・夜21時）から書き、重複ガードを通る。
テスト `server/beliefProofRotation.test.ts`。

**9/21 の当日補充で実物も確認ずみ**：投稿 #1768（10:08 作成・承認待ち・10:10 予定）
「『スポーツのケガは、少し休めば大丈夫』。そう思われがちです。…土浦市で多くの学生を見てきましたが、
早めのケアが復帰への近道です。」＝「11年」も「揉んでもムダ」も入っていない。
承認のご案内メールも 10:08:44 に送信ずみ（email_logs #545）。

## 2日続けて届かなかったときのお詫び・お願い・補填（11c6efc・2026-09-21 三上様指示）

> 「2日連続でスキップされてしまった場合は、こちらから『追加情報でこれを送ってください』と提案し、
>   それを送ってもらって、その内容が反映できるようにしてください。お詫びで補填するようにしてください」

- 連続ゼロの日数を数える（`zeroPostDays`／`zeroPostDate`。1件でも作れたら0に戻す）
- 2日続いたら、お詫び＋「これを送ってください」をLINEへ1通（未連携ならメール）。
  お願いする項目は `assessMaterialDepth` の空欄から効く順に3つまで。同じお願いは**7日に1度**まで
- お願いのあと7日間は、**短いお返事でも材料として受ける**（`looksLikeMaterialOnly` は並び方で見分けるため、
  お願いへの素直な一文が自動応答に横取りされて消えていた）
- **お詫びの補填**（`apologyShortfall`）。届かなかった本数をためて1日＋1件で必ず返す。
  `deletedShortfall` とは分けて持つ（混ぜると「スパム判定の可能性」という別の説明が伝わるため）
- 運営にも通知し、朝の報告に出す

マイグレーション 0092（本番適用ずみ・`[DB] Applied migration: 0092_zero_post_material_ask.sql` を確認）。
テスト `server/zeroPostApology.test.ts`。

**★まだ誰にも送っていません。** 香取様は 9/21 に当日補充で1本お届けしたので連続ゼロは切れており、
最短で発火するのは「9/22・9/23 と2日続けてゼロだったとき」です。
**文面を一度 三上様にご確認いただきたい**（`shared/materialDepth.ts` の `zeroPostApologyNotice`）。

## 香取様へのお返事と補填（2026-09-21 10:28・三上様の承諾を得て送信ずみ）

- ご質問 #41「昨日は投稿の案が届いていません」へ、**管理画面の返信と同じ経路でお返事を送信**
  （`pushTextTo` → `updateSupportQuestion` の順。送信できたときだけ記録する形にしたので二重送信はない）。
  `repliedAt = 2026-09-21 01:28:54 UTC`。文面は `scripts/ops/announcements/2026-09-21_katori_reply.txt`
- 文面に書いた数字は実データで数えたもの：**9/3からの19日間のうち5日間（9/10・16・17・18・20）は1本も作れていない**、
  公開できたのは5本（うち2本はその後Threads側で削除）、お客様ご自身の見送り8本、仕組みの見送り5本
- **お詫びの補填として `apologyShortfall = 5` を acc21 に積んだ**（三上様の承諾あり）。
  acc21 は 9/25 まで冷却中で補填が乗らないため、**実際に増えるのは 9/26 から**。
  お客様にもそのとおりお伝えしてある（「9月26日以降に1日1件ずつ」）
- なお acc21 には `deletedShortfall = 2` も残っている。`rampForAccount` は消えた分を先に返すので、
  9/26 から7日間ほど「契約1件＋1件」が続く見込み

## 承認待ちで止まらないようにした（2026-09-21 23:30 三上様指示）

> 「こちらの承認なしに夜中の1時になったら点検をスタートし、完結させるようにしてください」

**実行履歴で確かめた**：起動は毎回01:01で正常。止まっていたのは中身で、所要時間が37分〜**26時間**とばらついていた。
`CODE/.claude/settings.local.json` に**手作業で承認したBashルールが460件**積み上がっていたのが証拠。
Mac mini は `pmset sleep 0`＝スリープしない構成なので、**スリープは原因ではない**。

直したもの：
- `CODE/.claude/settings.json`（新規）に `permissions.defaultMode = "bypassPermissions"`。
  ユーザー設定に `skipDangerousModePermissionPrompt: true`。管理者設定は無いので有効
- この手順書（SKILL.md）の「守ること」の先頭に、**無人で完結させる決まり**を足した：
  質問しない／判断待ちは実装も送信もせず報告に書くだけ／**04:30で新規着手をやめる**／
  **05:00を過ぎたらデプロイしない**／待ち時間に別の作業を進める
- 同じく手順書の反映手順から `curl .../api/v1/deploy` を外した。**pushだけでよい**
  （サーバーのcronが5分おきに拾う。手で叩くと二重デプロイになる）

**設定はセッション開始時に読まれるので、走っているセッションには効かない。今夜01:01の回から有効。**

**今夜の回で最初にやること**：未pushのコミット5件を確認して反映する
（うち `795512b` は朝の点検セッションのもの。中身を読んでから一緒に push すること）。

## 明朝（9/22）に必ず見ること

- [x] ~~19時以降のずらしが止まっているか~~ → **9/22 未明に本番ログで確認ずみ。効いている。**
      `[AwaitingSlide] post=1768 user=3500 → ずらさない（19時以降。日付が変わったら見送り）` が3回出ており、
      投稿 #1768 は翌日へ送られず、9/22 00:00 に見送りになった（旧：翌朝へ送られて必ず見送り→その日ゼロ）
- [x] ~~登録直後の画面の「コピー」が押せること~~ → **9/22 未明に確認ずみ。押せる。**
      ローカルQAで新規登録から `/dashboard` まで実際に通し、**320px・375px・390px**の3つで
      「コピー」「公式LINEを友だち追加」の両方が画面内にあり、何にも覆われていないことを実測（横はみ出し0）。
      本番のコンテナの中の `Dashboard-BLuI7c7k.js` にも直した形（`flex-wrap`／`sm:tracking-[0.3em]`）が入っており、
      古い `min-w-[9rem]` は0件であることを確認した
- [x] ~~**香取様（acc21）に 9/22 の投稿が作られたか**~~ → **作られた。繰り越しの直しは効いている。**
      投稿 #1795 が 9/22 06:13 に作られた（`createdAt` 2026-09-21 21:13 UTC）。
      **ただし 06:17 に見送りになっている**（`errorMessage` は空＝システムの見送りではなく、
      **お客様ご自身が「違う」を押された**）。作られてから4分。
      ＝ 下の「承認されないほうが残りの問題」を裏づける3日目の実例。
- [ ] **香取様は「承認されない」ほうが残りの問題**（9/22 未明に実測）。
      9/21 は投稿 #1768 が 10:08 に作られ、内容も直った形（「11年」も「揉んでもムダ」も無い）で、
      承認のご案内メールも届いている。それでも**ご承認がないまま 9/22 00:00 に見送り**になった。
      直近14日の見送り11件のうち**8件はご自身で「違う」を押されたもの**。
      ＝ もう「投稿が作れない」問題ではなく、**「作ったものをご承認いただけない」問題**に変わっている。
      公開前確認を外すか、内容のすり合わせを一度Zoomでするか、**三上様のご判断をいただきたい**
- [x] ~~`node scripts/ops/reject-reasons.mjs 24` が動くか~~ → **動いた。9/22 06:00 の生成で82件記録された。**

## ★9/22 朝の実測（反映後の初回。次の夜間整備はここから始める）

反映（33eda37）は **9/22 01:30 ごろ**に完了し、6時の生成はこの版で動いた。
`scripts/ops/reject-reasons.mjs 24` の初回の結果：

| 理由 | 件数 | うち枠を捨てた |
|---|---|---|
| naturalnessReview | 31 | **7** |
| angleGuard | 29 | 0 |
| duplicatePhrase | 16 | **4** |
| duplicateHookNumber | 5 | 0 |
| healthClaimGuard | 1 | 0 |

**★手順書 §3.45 の「同じ理由で3回落ちて投稿ゼロ」に当たったのは1件**：
**株式会社プレステージ（@esthe_prestige_r）が naturalnessReview で3回**枠を捨てている。
**生成側（プロンプト・自然化）を見る対象。ガードは緩めない。**

そのほか気になるもの（枠を捨てた数の多い順）：
小西様 @hatsukaichitenjinseitai（naturalnessReview 2・duplicatePhrase 1）／
三上様 @moveact_pilates_tamashima（duplicatePhrase 2）／滝本様（naturalnessReview 1）／
氷見様（duplicatePhrase 1）／髙木様（naturalnessReview 1）

くり返している中身では **angleGuard の「切り口の印が無い」が9種類で計24回**。
ラベルだけ付いて中身が伴わない状態が広く出ている（ただし angleGuard は最後の作り直しでは
公開する作りなので、**枠は1つも捨てていない**）。

### 9/22 朝の生成そのものは正常

- 46本／23アカウント（pending 19・承認待ち 24・見送り 3）
- 1本も作られなかったのは acc9（Meta Reviewer・自動投稿OFF）と
  acc33（プレステージ様 `esthetic.salon_prestige`・お店の情報が未紐づけ＝仕様どおり止めてご案内）の2つだけ
- 見送り3件はすべて**お客様ご自身が「違う」を押されたもの**（acc21 ×1・acc22 ×2）

## 忘れると事故になること

- **本番のログは短い時間で消える。** 9/21 01:16 に**理由の分からない再デプロイ**が走り（同じコミット 7e70eff・
  `auto-deploy.log` には記録なし・コンテナは作り直し）、それ以前のログが全部消えた。
  9/19 20時にも同じことが起きている。**数えたい検査は、ログではなくDBに印を残す形にする。**
- ~~06:00 の angleGuard 初回~~ → **9/20 未明に確認ずみ。契約本数を下回った方はいない**
- ~~account_ramp_2026-09-19 が13名~~ → **13名で一致（確認ずみ）**
- ~~津留様のフリー行き止まり~~ → ローカルQAで通しで確認ずみ（c004404 は効いている）。
  本番で津留様が実際に押されたかは未確認
- **斎藤様（10043・pro_campaign・9/16課金）は、9/19 06:23 に「これから南口院のアカウントを作って連携させておきます」と
  お送りくださったまま、9/21 01時の時点で Threadsアカウントが0件**（`threadsAccounts WHERE userId=10043` が空）。
  朝7:40のご案内（`no_account`）は 9/20 に届いている。**案内では動かれていない＝お声がけの対象**

---

## 三上様の判断待ち（送信も変更もしていません）

### ~~R. プレステージ様の採用の材料~~ → **2026-09-21 に完了（三上様の承諾あり）**

**訂正**：9/20 の申し送りに「お店の情報には1文字も入っていない」と書いたのは**誤り**でした。
`projects.updatedAt` は **2026-09-19 19:20 JST**（材料が届いた9分後）で、
N1顧客像・実績・強みには**すでに入っていました**。#40 は 9/21 09:19 に三上様が対応済みにされています。

9/21 に、まだ入っていなかった分だけを足しました（変更前の値は
`scripts/ops/announcements/2026-09-21_prestige_before.json` に保存）。

| 項目 | 前 | 後 |
|---|---|---|
| お客様から言われた言葉 | 空欄 | 入社された方の生の声3つ |
| 信条 | 「技術を覚えるのが大変」（信条ではなく不安） | 代表のお考え3つ |
| 地元の言葉 | 空欄 | 横浜駅・神奈川駅・川崎駅・京急川崎駅・横須賀中央駅（承認ずみ） |
| 選ばれる理由 | 2行 | ＋各店の雰囲気3行 |
| N1顧客像 | 場面3つ＋生の声3つ | 場面3つだけ（生の声は上へ移動） |

- **生の声を N1 から「お客様の言葉」へ移した**のは、同じ文が2か所にあると同じ投稿に戻る原因になるため。
  「お客様の言葉」はプロンプトで「★最優先。この生の言葉をそのまま投稿に1〜2個使う」と指示している欄
- 地元の言葉は `localTermsConfirmedAt` が無いと投稿に使われない作り。
  **駅名はお客様ご自身が #40 に書いてくださったもの**なので、承認ずみにした
- 9/19以降の✕7件のうち4件がプレステージ様の採用投稿（具体のない文）だった。明朝の投稿で改善を確認する

### S. 「揉んでもムダ」型の言い方に検査を足すか（2026-09-20・実測つき／2026-09-21 に測り直し）

- 9/17 以降にお客様が✕にされた投稿9件のうち4件が、比嘉様・香取様の
  「強く揉むだけでは、身体は根本から変わりません」「痛い場所だけ揉んでも、一時的になりがちです」型。
  固定投稿の生成でも同じ型が出る（9/20 未明に実測：「どんなに高価な化粧品を使っても、
  表面的なケアだけでは一時しのぎで終わることがほとんどです」）。切り口の指示では禁じているが、効いていない。
- **直近30日で測った**（1016本）：この型に当たるのは **53本（5.2%）**。
  そのうち✕は4本（7.5%）、◯は1本。全体の✕は41/1033（4.0%）なので**1.9倍**だが、
  **44本はそのまま公開されて何も言われていない**。検査を足せば5%が作り直しになる。
- 数としては弱い信号です。**足すか・様子見かのご判断をお願いします**（夜間整備では足していません）。
- **2026-09-21 に測り直した**（言い回しの拾い方を広げたので、上の 9/20 の数字とは直接くらべられません）。
  直近30日の自動投稿 **1,087本**のうち、この型に当たるのは **79本（7.3%）**。
  - 型の✕：**10本 / 79本＝12.7%**
  - 型でない投稿の✕：32本 / 1,008本＝**3.2%**　→ **約4倍**
  - 全体の✕42本のうち **10本（24%）がこの型**。一方、型の◯は3本、**66本はそのまま公開されて何も言われていない**
  - 9/21 未明の固定投稿の生成でもまた出ました：「肩こりは、揉むだけでは一時的に楽になっても、すぐに元に戻ってしまいます」
- 検査を足すと、作り直しが約7%増えます。**足す／様子見のご判断をお願いします。**
- **2026-09-21 09:20 追記（話が変わりました）**：香取様は今朝、この型が原因で**投稿がゼロ**になりました。
  AIが毎回この型に戻り、ご自身の過去の投稿とぶつかって3回とも差し戻されたためです（上の最優先の項を参照）。
  **「✕が増えるかどうか」ではなく「投稿が出なくなる」問題になっています。**
  検査を足すかどうかとは別に、**生成側でこの型に戻らないようにする**ことを今夜やらせてください。

### A5. 大木慎也様は「有料」ではなかった（2026-09-19・こちらの誤り。お声がけは取り下げ）

- 三上様に「有料の方だけ先に」とお伺いした際、大木様を有料として並べたのは**誤り**。
  大木様（7961）は `status='trialing'`・**UnivaPayの決済IDなし・課金回数0**＝**PROST2026 の期限なしトライアル5件**の1人で、
  9/19 に三上様が「このクライアントは無視で大丈夫」と決めた対象だった（juria様・佐々木様も同じ）。
  **お声がけの文面は用意しない。ご承諾を仰ぐこともしない。**
- 原因：朝の点検スクリプトの「契約中（planId が free 以外）」に trialing が混ざる。これをそのまま「有料」と読んだ。
- **これから「有料の方」を数えるときの条件**：
  `subscriptions.status='active' かつ (univapaySubscriptionId か stripeSubscriptionId がある)`。
  9/19時点で12名。そのうち投稿が止まっているのは**斎藤様1名だけ**（他11名は本日も生成ずみ）。
- 大木様の isDemoMode は日中に 0 にしてある。これは**お声がけではなく、製品として当たり前の朝のご案内が届く状態に戻しただけ**。
  トライアルの方に朝の案内を出したくない場合は戻せる（ご指示があれば）。

### A1. 冷却（投稿が1件消えたら7日間1日1件）を続けるか（2026-09-19 三上様ご質問）

- 三上様「投稿が1回消されたぐらいで冷却する必要は本当にあるの？」→ **実データを添えて朝にご報告済み。ご判断待ち。**
- 事実：冷却の仕組みができた 9/12 以降に冷却へ入ったのは 4アカウント5回。うち **1件消えただけで入ったのは香取様の2回**
  （9/13・9/17）。冷却後にMetaから実際の制限を受けたアカウントは 0。停止まで行ったのは仕組みができる前の だいご接骨院 1件。
- 弱点：健全性点検は「投稿が消えた」しか見ておらず、**ご本人が消したのか Meta が消したのかを区別できない**
  （server/accountHealthJob.ts の /me 照会は 404 を返すだけ）。ご本人が気に入らず消した1件でも、7日間 1日1件になり、
  「スパム判定の可能性」というお知らせが飛ぶ。
- 直すならの案（コードは書いていません）：
  1. 1件だけ消えたときは冷却に入れず、**LINEで「この投稿はご自身で消されましたか？」と伺う**。
     「いいえ」または7日以内に2件目が消えたときだけ冷却。
  2. 冷却は7日→3日に短くし、本数は契約の半分（3件契約なら2件）まで。
  3. 今のまま（いちばん安全だが、ご本人が消しただけでも1日1件になる）。

### A0. 冷却中なのに1日2件になる食い違い（2026-09-19 朝に発見・修正待ち）

- `server/autoPostScheduler.ts:1244-1259` の自動繰り越し（`carryOverCount`）が、冷却の頭打ち
  （`server/accountRampCheck.ts:60` の 1日1件）を見ずに後から足している。
  9/19 朝、香取様（acc21・冷却〜9/25）と しっとる公式（acc14）が 1件→**2件**になった。
  `docs/safe-operation-rules.md` の「冷却中は補填も乗せない」と食い違う。
- **A1 のご判断が先**（冷却そのものを緩めるなら、直し方が変わる）。今のまま続けるなら、
  `carryOverCount` の呼び出し側で `inCooldown` のときは 0 にするだけ。テストは `shared/accountRamp.test.ts` に足す。

### A4. コメントの「返信しない（スルー）」をWeb画面にも置いた（2026-09-19 三上様指示・反映待ち）

- 三上様「勧誘や出会い系のコメントに関しては、来る場合もあるので、コメントが来たらスルーできるボタンも配置して」
- LINEのカードには 9/17 から「返信しない」がある（反映済み）。**Web画面（コメント管理）には無かった**ので足した。
  - `client/src/pages/CommentManager.tsx`：各コメントに「返信しない」。押すと一覧から外れ、
    「返信しないことにしたコメント（n件）を表示」から元に戻せる。勧誘・出会い系と見分けたものは
    黄色の注意書きを出し、「返信しない」を主ボタンにする（見分けは `shared/commentSpam.ts`＝LINEと同じ）。
  - `server/routers.ts` の `hidden.hide/unhide` と `server/db.ts` の `getHiddenItems` に `comment` を足した
    （hiddenItems テーブルの itemType は varchar なので**マイグレーション不要**）。
- ~~375px のはみ出し確認が未実施~~ → **2026-09-20 未明に実施ずみ。問題なし。**
  ローカルQAに勧誘1件・ふつう2件のコメントを出して 375px で確認した：横はみ出し 0・JSエラーなし。
  勧誘のカードだけ黄色の注意書きが出て「返信しない」が主ボタン（ふつうのカードは「AI返信を生成」が主）。
  「返信しない」で一覧から外れ、「返信しないことにしたコメント（1件）を表示」→「元に戻す」で戻ることも確認。

### A2. 体験ページ（ログイン不要）の文の「他の方法をけなす言い方」（2026-09-19）

- 本番の体験ページで「揉んでも無駄です」「肩こりに湿布、やめてください」のような、他の方法をけなす文が出る。
  毎日の投稿の切り口指示では「同業・特定の方法は絶対に批判しない」と禁じているが、体験ページの生成には効いていない。
- 健康表現（結果・原因の断定、診断・治療の語）は 9/19 未明に体験ページにも掛けた。**けなす言い方を止める型を足すかはご判断**
  （足すなら直近30日の実データで影響を測ってから）。

### A3. フリープランの導線の方針（2026-09-19）

- 文言は事実に合わせた（お試し「保存して自動投稿を始める」→「無料登録して、あなたのお店で試す」ほか）。
- ただし「体験→登録→フリーは自動投稿なし」という流れ自体は残っている。フリープランで何をさせたいか（お試しの延長か、
  有料へ進める入口か）で、体験ページの見せ方が変わる。**方針をお決めいただければ、それに合わせて直す**。
- **9/19 03:10 反映ずみ**：LINEでフリープランの方が行き止まりに当たる3か所（設定完了・自動投稿ON・あと2問完了）を、
  「いまのプランでは動かない→7日間無料で始めると明日の朝6時に1本目が届く→[プランを見る]」に変えた。
- **PROST2026（forever_free・期限なしトライアル5件）は三上様の指示で対象外**（9/19「このクライアントは無視で大丈夫」）。
  朝の点検の印は「カード登録つきトライアル中で公開0本」だけ。いまは該当0件（本番DB：trialing 5件すべて期限なし）。
- 未着手の案：フリーでも「最初の1本」を生成して見せる／登録後3日・7日のLINEでの声かけ。ご判断待ち。


### B. 佐々木竜也様（9/9ご登録・pro）の登録内容が業種と食い違う

- 業種「コンサルタント」に対し、お客さん像に「デスクワークの会社員」、強みに「専門特化（〇〇専門）」と
  治療院の言葉が入っている。9/13の朝の点検にも同じズレが出ている。
- 業種ズレの重複通知は止まっている（`industryMismatchNoticeKey`）。
- 文面は用意ずみ。**三上様の承諾をいただいてから**お送りします。

### C. Coolifyの環境変数 `RESEND_FROM_DOMAIN=resend.dev` を消す（運営作業）

- 本番の実行中コンテナは `threads-studio.com` を正しく読めている。重複しているのは
  **プレビュー用（`is_preview: true`）の1件**（uuid `nioepcaeeyhgczsx70vyhydx`）。
- 9/9 01:19 のデプロイ中に実際にこちらが拾われ、お客様宛メールが1通落ちている。
  環境変数の削除は本番設定の変更なので夜間整備では触っていない。

### D. 自動投稿の必須条件を減らすか（2026-09-11）

- 「はじめの設定」は5問で終わるのに、自動投稿は今も **お客さん像（target）と強み（strength）** を必須にしている
  （`autoPostScheduler` の `eligibleProjects`）。
- 9/13の通し確認でも同じ：5問を終えた直後は target と strength が必ず空になる。
  案内は「あと2問だけ答える」で1本道になっており、進み具合の見出しも「（あと2問）」に直した（9/13）。
- 残るご判断：必須条件そのものを「業種・地域・お悩み」まで減らすと5問だけで投稿が始まるが、
  強みが無いぶん投稿は弱くなる。**要ご判断**（夜間整備では仕様は変えていない）。

### F. サーバーのディスク掃除 → **2026-09-15 未明に実施。急ぎではなくなった**

- 9/15 01:20 時点で **44%（41G/99G）**。9/14 の 55% から下がっており、注意ライン70%には遠い。
- 指示どおり `docker image prune -af --filter until=168h` と `builder prune` を流したが、
  **回収できたのは 18.9MB だけ**だった。理由は単純で、毎晩デプロイしているので
  積み上がったイメージが**どれも7日より新しく、168時間の条件から外れる**ため。
- いま `docker system df` では **イメージ 30.3GB のうち 20.8GB が回収可能**。
  回収したければ条件を短くする（例：`until=48h`。稼働中のコンテナのイメージは消えない。
  切り戻しは Coolify が git から作り直すので古いイメージは要らない）。
  ただし**保存期間を変えるのは運用方針の変更**なので、夜間整備では触らず 168h のままにしてある。
  **ご判断**：48時間に縮めてよければ、次の夜に流して 20GB ほど空けます。
- 注意ライン70%・変える基準「掃除しても70%超」は `docs/scaling-thresholds.md` に追記ずみ（9/14）。

### K. 森様への返信の文案（未送信・承諾待ち）

※ すでにご自分でつなぎ替えを済まされたので、手順の説明はしません。

> ご連絡をありがとうございました。また、お返事が遅くなり申し訳ございません。
>
> 「連携を解除しても、もう一度つなぐと元のアカウントしか出てこない」という点は、
> こちらの作りの問題でした。**9月17日に直してあります。**
> 「Threads連携」の画面に「別のアカウントにつなぎ替える（Threadsのログイン画面からやり直す）」
> を追加し、押すとThreadsのログイン画面が必ず開くようにしました。
>
> すでに交通事故専門のアカウントへのつなぎ替えが完了していることを確認しております。
> ご不便をおかけしました。
>
> なお、**つなぎ替えた直後のアカウントは、最初の10日間だけ投稿の本数を抑えます**
> （1〜5日目は1日1件、6〜10日目は1日2件、11日目からご契約どおり）。
> Threads側で新しいアカウントが止められるのを防ぐためで、抑えた分は後から取り戻します。
> 不具合ではありませんので、ご安心ください。
>
> ご不明な点は、このままトークにお送りください。

※ 慣らし運転の説明は 9/19 朝7:40の一斉お知らせ（`account_ramp_2026-09-19`）でも森様に届きます。
　重ねて届いても矛盾しない文にしてあります。

### Q. 佐々木様：2つのアカウントに、内容が入れ替わって公開されている（2026-09-19 実測・急ぎ）

朝の点検の「★お店の情報が未紐づけ1件・取り違えの恐れ」の中身を実データで確かめた。**実際に起きていた。**

- `shittoru.1203`（お仕事）に **9/18 #1615「実は、夜遅くにApexを楽しむ人、結構いるんです！」**（ゲームの内容）
- `tama.yurazoku`（珠由良族・9/17連携）に **9/17 #1594「倉敷市で店舗を経営しているデスクワークの方へ」**（お仕事の内容）
- どちらも**公開ずみ**

原因：`tama.yurazoku` の `defaultProjectId` が空で、ひとつしかない「お店の情報」が両方で使われている。
その「お店の情報」の中身も、お仕事（倉敷・集客）とゲーム（珠由良族・Apex）が混ざっている。

**お客様の設定は変えていない。** 文面は用意ずみ：
`scripts/ops/announcements/2026-09-19_sasaki_account_project_draft.txt`
**三上様の承諾をいただいてからお送りします。**

### M. 体験ページのCTAに「診断」と出る（2026-09-19 未明に発見・直していません）

ログイン不要の体験ページのCTAに「あなたの根本原因、**LINEで診断できます**」と出た。
整体院が「診断」と書くのは医師法の観点で避けたい言葉。健康系のお店では
「診断」→「チェック」「確認」に置き換える型を足すか（**実測してから**）。
同じく「揉んでも無駄です」のような他の方法をけなす言い方も、切り口の指示では禁じているが
体験ページには効いていない。

### N. 体験ページの導線の食い違い（2026-09-19 未明に発見・直していません）

体験ページの「保存して自動投稿を始める／カード不要」→ フリープランは自動投稿なし・保存されない。
文言と方針の判断が要る。

### O. 文体のお手本が空のまま3日以上（2026-09-20 に数え直し）

**アカウント単位で数え直した**（そのアカウントが実際に使う「お店の情報」の styleSamples を見る。
`defaultProjectId` が空のアカウントは、その方の1つだけの「お店の情報」に落ちるので、そちらを見る）。

| お名前 | アカウント | 連携からの日数 |
|---|---|---|
| 滝本大文 様 | takimoto_sora | 37日 |
| 三上尚志 様（自社） | shittoru_official | 31日 |
| 髙木　基太郎 様 | miraiseitai_kanazawa | 18日 |
| 株式会社プレステージ 様 | esthe_prestige_r | 16日 |
| 比嘉優 様 | haisaiseikotsuin | 13日 |
| 佐々木　竜也 様 | shittoru.1203 | 11日 |
| juria 様 | desire_6981 | 10日 |
| 梅原悠佑 様 | yusuke_seitai | 4日 |

**空ではない方**（前回この一覧に入りかけたが、実際には入っている）：香取様367字・氷見様1409字・小西様171字・
三上様 moveact_pilates 245字・髙木様 miraiseitai.diet 442字・梅原様 daigo.sekkotsuin 718字・岩根様1390字・小林様241字。

「次にやること」では毎朝ご案内が出ている（「理想の投稿を貼る」）。
それでも8アカウントが空のままなので、**案内の出し方を変えるか、こちらでお声がけするか**のご判断をお願いしたい。
お手本が無いと、投稿の口調が全員似た形に寄る。

**2026-09-21 に数え直したところ、8 → 12アカウントに増えています**（新しく連携された4つが全部空のため）。
上の8つはそのまま、加えて：森様 `angyomori.jiko`（3日）・佐々木様 `tama.yurazoku`（3日）・
津留様 `tsurutaka_seikotsuin`（3日）・プレステージ様 `esthetic.salon_prestige`（1日）。
**連携した直後にお手本をお願いする形にしないと、増える一方**です（いまは毎朝の案内だけ）。

### ~~P. 岩根様：材料は渡っているのに、投稿が使っていない~~ → **2026-09-21 に解決を確認。検査は足しません**

9/18 に足した「N1顧客像を日替わりで1つ指定する」（`focusN1`）＋ 9/19 からの angleGuard で、
**9/19・9/20 の7本すべてが、それぞれ違うお客様の場面を使っている**（実データで確認）。

| 日 | 投稿 | 使われた場面 |
|---|---|---|
| 9/19 | #1661 behind_scenes | お茶席 |
| 9/19 | #1666 local | 講演会で登壇 |
| 9/19 | #1670 change_story | 園遊会 |
| 9/19 | #1676 surprise_fact | お琴の発表会 |
| 9/20 | #1710 local | 海外へのお着物 |
| 9/20 | #1715 deep_worry | イベント |
| 9/20 | #1717 personality | 講演会に登壇 |

9/18 は3本中1本だけだったので、**1／3 → 7／7 に変わった**。
「創業130年・本物の正絹」は今も毎回入るが、**毎回ちがう場面に結びついている**ので狙いどおり。
**場面が入っているかを見る検査は足しません**（すでに効いているため）。

---

## 2026-09-19 未明（夜間整備）に直した分

### 1. 勧誘・出会い系のコメントに「返信しない」が無かった（ご質問 #37 の原因）

9/17 梅原様「こちらのコメントは明らかな勧誘、出会い系的なコメントでしたので返信しませんでした。」

**知識ではなく画面が原因だった。** コメント返信カードのボタンは
「この文で送る」「Threadsアプリで返信する」「文案を作り直す」の3つだけで、
**返信しないという選び方が無かった**。そのため返さないと決めた方は文章で書くしかなく、
自動応答は答えられず（`aiConfident=0`）担当者送りになっていた。
さらにカードには、勧誘コメント宛の「お礼と共感」の文案が入っていた。押せば勧誘アカウントへ返信してしまう。

- `shared/commentSpam.ts`（新規）… 勧誘・出会い系・副業の誘いを見分ける。**迷ったら「勧誘ではない」に倒す**
- 勧誘と分かるコメントには**文案を作らない**（AIも呼ばない）。カードは「勧誘・出会い系のコメントのようです」＋「返信しない」だけ
- **「返信しない」は、ふつうのコメントのカードにも必ず置く**
- カードが古いまま「この文で送る」を押されても、**送る直前にもう一度見分けて送らない**
- 入れた場所：3か所すべて（`dailyOpsJobs` の巡回・`threadsWebhook` の即時・LINEの「文案を作り直す」）
- ルール文書 `docs/safe-operation-rules.md` に追記。テストは `server/commentSpam.test.ts`

※ 梅原様のご質問文そのものは、9/18 に足した知識で**すでに正しく答えられる**ことを実測した
（ローカルQAで投げ直し：「返信されないのが正しいご判断です」と `confident` で返る）。

### 2. 投稿の材料が、自動応答に横取りされて消えていた（申し送り I の根本の直し）

自動応答は**12字以上の文章をすべてご質問として受け取る**ため、材料をお送りいただいても
AIが自信をもって案内して終わり、`aiConfident=1` なので担当者にも届かず、材料はどこにも残らなかった。

**実測した取り違え2つ**（ローカルQAに本番と同じ内容を作って確認）：
- 症例3件（本番のご質問 #3 の文章）→「はじめの設定からご自身でご登録ください」
- プレステージ様へお願いした形の材料 →「**理想の投稿を貼る**から登録してください」
  ＝**文体のお手本のご案内**。お手本に入れても中身は投稿に使われないので、書いてくださった内容が活きない

- `shared/requestKind.ts` に `looksLikeMaterialOnly` を足し、**自動応答より先に**受け止める。
  その場に「実績として登録」ボタンを出す
- 治療院の言葉が1つも入らない材料（採用の発信など）は、**並び方**で見分ける
  （番号つき3行以上、または「」の引用2つ以上）
- **横取りしていないかを本番の実データで確かめた**：お問い合わせ33件すべてに当てて、当たったのは
  **#3（症例3件）の1件だけ**。AIが自信をもって答えられていたご質問を奪ったものは**0件**
- 「実績として登録」を押していただけなくても消えないよう、お問い合わせの記録に
  `category='投稿の材料'` で残す。**担当者への通知（push）は出さない**
  （材料は困りごとではないので、通知すると本当に困っている方のご連絡が埋もれる）
- `shared/productKnowledge.ts` に「文体のお手本」と「投稿の材料」の読み分けを足した。
  ご案内の書き方も決めた：**すでにトークに送っていただいているので「もう一度送ってください」とは言わない**
- テストは `server/requestKind.test.ts`・`server/threadsAppKnowledge.test.ts`

### 通し確認でわかったこと（ローカルQA 3100・本番には触れていません）

- 会員登録 → ログイン → 料金プラン → **有料4プランすべて**、ポップアップを遮断した状態
  （`--block-new-web-contents`）でも**同じタブで決済ページ（checkout.gopay.jp）まで進める**。
  金額もライト4,980／プロ9,800／ビジネス29,800／代理店55,000で一致。JSエラーなし
- 375px の主要22ページに、横スクロール・JSエラー・空表示なし
- 「アカウント作成」「ログイン」「7日間無料で試す」が**何かに覆われていないこと**を実測。
  ※ 一度「覆われている」と出たのは**確認スクリプト側の誤り**で、ボタンが画面の下にあるだけだった
  （`elementFromPoint` は画面外だと `null` を返す）。**先に `scrollIntoView` してから見ること**
- LINEのボタン83種すべて応答あり（無反応・THROW・「うまく受け取れませんでした」・未連携の案内はゼロ）
- 「はじめの設定（まず5つ）」は 1／5 → 2／5 → 3／5 → 4／5 → 5／5 → 確認 →「この内容で登録する」まで通る。
  もう一度「はじめの設定」を押すと、前回の答えが入った状態で出る
- 固定投稿は 作る → 品質検査 → 案（400字ほどの長文）→ ピン留めの手順案内 → 「ピン留めしました」の記録まで通る
- `npx tsc --noEmit` エラー0／`npx vitest run` は **116ファイル・1015件成功**（失敗0・16件スキップ）／
  `pnpm build` 成功、成果物を `node dist/index.js` で起動して health 200

### 直していない（原因を確かめたうえで「仕様どおり」と判断したもの）

- **承認待ちのまま止まって見える投稿2件**（#1551 髙木様 9/16・#1555 梅原様 9/16）は
  どちらも `angle='pinned'` ＝**固定投稿の下書き**。日をまたいだ見送りの対象外で、
  7日たつと `cancelStalePinnedDrafts` が取り下げる（仕様どおり）。
  ただし**お二人とも9/16に固定投稿を作りかけて、そのまま止まっている**（セミナーの日）。朝の報告に載せた
- **`adminReviewRequired=1 AND adminReviewAt IS NULL` が25件**出るが、
  中身はすべて公開ずみ・見送りずみで、**運営の確認を待って止まっているものは0件**。
  `adminReviewAt` は承認時に埋まらない作りになっている（表示には使っていない）

---

## 2026-09-19 朝に見ること（今夜の反映分と、前夜に入った angleGuard の初回）

- [ ] **angleGuard の初回**（9/19 06:00 の生成が最初）。
      `docker logs --since 12h <app> | grep -c "angleGuard:"` ＝ 生成本数の3〜4割が目安。
      `generated` が契約本数を下回っていないこと（最後の作り直しは公開する作りなので0のはず）
- [ ] 岩根様（5443）の3本が、**それぞれ違うお客様の場面**になっているか
      （園遊会／文化勲章／講演会／海外訪問／お茶席／お琴／同窓会 のどれか）。
      2本以上がまた「創業130年・本物の正絹・敷居が高い」なら、上の **P** の検査を足す判断へ
- [ ] 三上様のアカウントの投稿が、切り口ごとに違う形になっているか（人柄の回に営業時間が無いか）
- [ ] LLM呼び出しが約1.4倍になっている見込み。1日分のコストを見る
- [ ] 07:40 の慣らし運転のお知らせが13名へ届いたか
- [ ] 勧誘コメントのカードが出た方がいれば、「返信しない」が押されているか
      （`docker logs | grep "cr=ignore"` ではなくLINEの記録側で見る）

---

## 恒常のメモ

- LINEの月間通数はコミュニケーションプラン（5,000通）。朝の点検に残量が出る（残り1割で警告）。
  通数切れのときは、お客様へは同じ内容がメールで届き、運営にも1日1回知らせが飛ぶ。
- ローカルQAは port 3100（`QA_SAFE_MODE=1`）。DBはローカルMySQLなので本番には触れない。
  ポートが埋まっていると勝手に 3101 へ逃げるので、確認の前に**どちらを見ているか必ず確かめる**。
  `handlePostback` / `handleFreeText` を直接叩く確認は、`BUILT_IN_FORGE_API_KEY` と `BUILT_IN_FORGE_API_URL`
  を Coolify の env から渡さないと自動応答が動かない（`.env` には入っていない）。
- LINEの総当たりは `scripts/ops/line-sweep.mjs`。**値を取るボタンは必ず値を付けた形で書くこと**
  （`c=st` のように前半だけ書くと、アプリは正しく「うまく受け取れませんでした」と返すので、
  こちらの書き間違いが不具合のように見える。2026-09-13 に一度そう見誤った）。
- ⚠️ **マイグレーションの適用済み判定はファイル名だけ**（`server/_core/index.ts`・中身のハッシュは見ない）。
  一度本番に当たったマイグレーションのファイルを後から書き換えても、**二度と実行されない**。
  直したいときは必ず新しい番号のファイルを足すこと。
  （0085 は本番未適用のうちに書き換えたので影響なし。ローカルだけ古い版が当たっていた）
- ⛔ **本番コンテナの中で `node -e 'require("/app/dist/index.js")'` を絶対にやらないこと**（2026-09-15 にやってしまった）。
  `dist/index.js` はサーバーの起動そのものなので、**本番のenvでアプリがもう1つ立ち上がり**、
  定期ジョブ（自動投稿・お知らせ）も初期化される。**二重投稿・二重送信になる。**
  今回は 01:44〜01:46 の約2分で気づいて止めた（次のジョブは6:00なので実害なし）。
  中身を見たいだけなら `grep` で十分。コンテナに `ps` も `kill` も無いので、
  消すときは `/proc` を読んで `sh -c "kill -9 <pid>"`（シェル組み込み）を使う。
- ⚠️ **`dist/index.js` の中の日本語は `\uXXXX` に変換されている**（esbuild の既定が ascii）。
  反映を確かめるときに日本語でそのまま grep すると **入っていても 0 件に見える**。
  逃した文字列に直してから探すこと（例：`同じ4問` → `\u540C\u30584\u554F`）。
- auto-deploy はサーバー側の cron（5分ごと・`/opt/scripts/auto-deploy.sh`）。**push すれば勝手に反映される**ので、
  手で `api/v1/deploy` を叩くと二重デプロイになる。トークンを入れ替えたら
  `~/.claude/secrets/coolify.token` と `/opt/scripts/.coolify-token` の両方を直す。
  確認は `tail /var/log/auto-deploy.log` に `Unauthenticated` と `[FAIL]` が無いこと。

---

