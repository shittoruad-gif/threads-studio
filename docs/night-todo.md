# 夜間整備への申し送り

朝の点検（`threads-studio-daily-check`）で見つけたが、日中は本番に反映しないため夜へ回したもの。
片づいた項目は消してよい。

---

## 三上様の判断待ち（送信していません）

### A. 小林武晴様の「強み・実績」の言い換え

- 9/8に `healthClaimGuard` が同日2件発動（`治る・改善の断定` を落とした userId=3200）。
  安全運用ルールの「同じアカウントで1日2件以上なら、はじめの設定の『強み・実績』に結果表現が多い」に該当。
- 文面は用意ずみ。**三上様の承諾をいただいてから**お送りします。

### B. 佐々木竜也様（9/9ご登録・pro）の登録内容が業種と食い違う

- 業種「コンサルタント」に対し、お客さん像に「デスクワークの会社員」、強みに「専門特化（〇〇専門）」と
  治療院の言葉が入っている。9/11の朝の点検にも同じズレが出ている。
- 業種ズレの重複通知は止まっている（`industryMismatchNoticeKey`）。
- 文面は用意ずみ。**三上様の承諾をいただいてから**お送りします。

### C. Coolifyの環境変数 `RESEND_FROM_DOMAIN=resend.dev` を消す（運営作業）

- 本番の実行中コンテナは `threads-studio.com` を正しく読めている。重複しているのは
  **プレビュー用（`is_preview: true`）の1件**（uuid `nioepcaeeyhgczsx70vyhydx`）。
- 9/9 01:19 のデプロイ中に実際にこちらが拾われ、お客様宛メールが1通落ちている。
  環境変数の削除は本番設定の変更なので夜間整備では触っていない。

### D. 自動投稿の必須条件を減らすか（2026-09-11）

- 「はじめの設定」は5問で終わるのに、自動投稿は今も **お客さん像（target）と強み（strength）** を必須にしている
  （`autoPostScheduler` の `eligibleProjects`）。9/11未明の時点で大木様・juria様（どちらもプロ・9/9ご登録）が
  強みだけ空のまま投稿ゼロだった。
- 9/11の夜間整備では**案内だけ**を直した（足りない項目を名指しして、その1問をその場で聞く＝`project_almost`）。
- 9/12の夜間整備で、ローカルQAで**新規のお客様の流れをそのまま再現して裏づけを取った**。
  「まず5問」を終えた直後のお店の情報は `target`（お客さん像）と `strength`（強み）が必ず空になる。
  つまり**新しくご登録いただいた方は全員、設定を終えてThreadsをつないでも投稿が1本も作られない**。
  設定完了の案内は「あと2問だけ答える」ボタンを出す形に直したので、その場で答えていただければ当日から動く。
- 残るご判断：必須条件そのものを「業種・地域・お悩み」まで減らすと5問だけで投稿が始まるが、
  強みが無いぶん投稿は弱くなる。**要ご判断**（夜間整備では仕様は変えていない）。

---

## 恒常のメモ

- LINEの月間通数はコミュニケーションプラン（5,000通）。朝の点検に残量が出る（残り1割で警告）。
  通数切れのときは、お客様へは同じ内容がメールで届き、運営にも1日1回知らせが飛ぶ。
- ローカルQAは port 3100（`QA_SAFE_MODE=1`）。DBはローカルMySQLなので本番には触れない。
  `handlePostback` / `handleFreeText` を直接叩く確認は、`BUILT_IN_FORGE_API_KEY` と `BUILT_IN_FORGE_API_URL`
  を Coolify の env から渡さないと自動応答が動かない（`.env` には入っていない）。

---

## ⚠️ 自動反映（auto-deploy）が丸1日止まっていた件（2026-09-12 に復旧）

- 経緯：サーバーの `/opt/scripts/auto-deploy.sh` に **Coolifyのトークンが直接書かれており**、
  9/10のトークン入れ替えで無効になっていた。9/11 01:40 以降は毎回 `{"message":"Unauthenticated."}` で失敗し、
  本番は `a7f3b14` のまま丸1日止まっていた。トークン自体は 9/12 00:59 に別の作業で差し替え済み。
- 夜間整備で作り直したこと（ここが最新）：
  - トークンの直書きをやめ、`/opt/scripts/.coolify-token`（600）を読むだけにした
  - これまで**失敗しても新しいSHAを「反映済み」として記録していた**ため、一度失敗すると
    次の5分で再挑戦せず黙って止まる作りだった。受け付けてもらえたときだけ記録し、
    失敗したら `[FAIL]` をログに残して次回もう一度試す
  - 9/12 01:30:03 の実行で成功を実測（Unauthenticated 以来はじめて）
- 影響していたのは threads-studio だけ（他15アプリは、この間に新しいコミットが無く無傷）。
- **恒常の注意**：これはGitに入っていないサーバー上の設定。トークンを入れ替えたら
  `~/.claude/secrets/coolify.token` と **`/opt/scripts/.coolify-token`** の両方を直す
  （スクリプト本体はもう直さなくてよい）。確認は `tail /var/log/auto-deploy.log` に
  `Unauthenticated` と `[FAIL]` が無いこと。

## 2026-09-11 未明にデプロイした分（本番反映済み・翌朝に見ること）

デプロイ 01:38 → 01:46 切り替え完了（`4557b15`）。実際に本番へ載ったのは `a7f3b14`（9/11 02時台）。

- [x] identityGuard は **19件 → 2件**（24時間）。プロンプトと検査の矛盾を直した分が効いている
- [ ] `naturalnessReview` の点数（9/11の24時間）：2/5 が14件・3/5 が25件・4/5 が11件・5/5 が43件。
      3/5 で落とすのをやめる修正（`510bfe5`）は 9/12 01:05 まで本番に無かったので、**9/12朝の数字で判断する**
- [ ] `voiceGuard` は24時間で2件（user 78 の「ますよ😊」）。氷見様・プレステージ様では出ていない
- [ ] 7:40 `[MorningDigest] 送信 N件`。8:30／8:35 に別の通知が出ていないこと
- [ ] 7:40 の直後、個別のお知らせ（お詫び）6通が届いたか（`shared/personalNotices.ts`）
- [x] 大木様・juria様は朝の点検で `project_almost`（あと1問）になっている
- [ ] 9/12 6時に `自動補填: 昨日届かなかった…` が出るか
- [ ] 10:00 の Meta AI呼びかけで `送信停止` が出て、対象の方に「再開する」ボタン付きの一言が1回だけ届くか

## 2026-09-12 未明に本番へ載った分（9/11日中の29コミット・要観察）

9/11 日中のコミットは 9/12 00:35 に push され、**01:05 に本番へ切り替わった**（`8af238ee`）。
9/12 6時の生成は、この新しい中身で動く最初の回になる。朝にここを見ること。

- [ ] `[AutoPost] 並列4で N人を処理（M分）` が出て、75分前後 → 20分前後に縮んでいるか。AIの 429 が出ていないか
- [ ] 最後の作り直しで 3/5 を通す修正が効き、**届かなかった枠が 19件から減っているか**
- [ ] 本文に `reservation_funnel` の切り口と「ご相談は…から」の誘導文が入っていないか
- [ ] `[AwaitingSlide]` が30分ごと／18:00 に `[EveningReminder] 送信`／comment_watch が15分ごと（429なし）

## 2026-09-11 朝（三上様「すぐに対処」：香取様の今朝0件）

- 9/11 6時：35枠中19枠が失敗。3回目の理由の半分以上が naturalnessReview 3/5。香取様は2枠とも0件 → 個別通知が条件で見送られた。
- 対処（ローカルの修正版で本番DBに対して実行）：最後の作り直しは3/5を通す（コミット 510bfe5・今夜デプロイ）。香取様の2件を当日補充で作成（#1297・#1298）→ お詫び文を手で送信（08:5x）。他11アカウントの不足分も当日補充を実行（結果は fill_all.log）。
- 気づき：
  - [x] 当日補充の承認カードの重複（香取様に3回）→ **2026-09-12 修正**。その回で作った投稿だけを承認カードにする
        （`getRecentAwaitingApprovalPosts` に `afterId`。生成を始める前のID最大値を目印にする）
  - [x] `fillStyleSamplesForAccount` が古い投稿を見本にしていた → **2026-09-12 修正**。
        まず直近6か月（足りなければ1年→全部）に絞ってから表示数で選ぶ。テスト `server/styleSamples.test.ts`
  - [ ] naturalnessReview が短い定型（「土浦で11年。」）に3/5を付け続ける。採点基準と生成プロンプトの整合を見る（週次リサーチの「決まり文句の流出はプロンプトの自己矛盾」と同根）
  - [ ] 固定投稿の下書き（source=manual, angle=pinned）が承認待ちのまま approval_reminder で毎日翌日へスライドし続けている（廿日市様：9/4から3件）。固定投稿の下書きは承認リマインドの対象から外す／7日で取り下げる。毎日メールが届いている可能性があるので確認
  - [ ] 手順書 docs/runbook-quality-shortfall.md と scripts/ops/fill-shortfall.mts を朝の点検 §2.7 で運用開始（9/12朝から）

## 2026-09-12 未明の通し確認で見つけて直した分

ローカルQA（port 3100）で、新しいお客様の目線でひととおり操作した結果。

- [x] **設定が終わった直後の案内が事実と違っていた**。「まず5問」ではお客さん像と強みが空のまま終わるのに、
      自動投稿はこの2つが埋まっていないと1本も作らない（`[AutoPost] Skipping user … - no project with required fields`）。
      それなのに「最初の投稿は、いまこの場で作ってお届けします」「明日から1日◯回…投稿を作ります」とお伝えしていた。
      → 足りない項目を名指しし、「あと2問だけ答える」ボタンでその質問に直接お通しするようにした。
      条件は `shared/autoPostRequirements.ts` に一本化し、生成側（`eligibleProjects`）も同じものを見る。
- [x] **「投稿を止めてください」が解約の案内に落ちていた**（「停止」という言葉だけで判定していた）。
      お休みしたいだけの方に解約手続きの説明が届くのは困るので、投稿の話だと分かるものは設定へお通しする。
- [x] **承認したときの「確認が終わり次第、公開します」をやめた**。2026-09-10 に「承認したらそのまま公開」へ
      変えてあるのに、LINE側だけ運営の確認待ちだと伝え続けていた（実際はもう公開予定）。
- 確認できたこと：会員登録 → ログイン → 料金プラン → 「7日間無料で試す」→ 決済ページ（checkout.gopay.jp）まで
  **ポップアップを遮断した状態でも同じタブで進める**。375pxで主要17ページに横スクロール・JSエラー・空表示なし。
  LINEのボタン47種すべて応答あり（無反応・「うまく受け取れませんでした」ゼロ）。「はじめの設定」は5問・20問とも最後まで通る。
- [ ] 残った気づき：「明日は休診です」のようなお知らせをLINEに送られても、いまは一般的なご案内に落ちる。
      香取様へ「出してほしい話題はそのまま送ってください」とお伝えしているので、受け皿を作りたい
      （実績として登録＝`c=addproof` は文言が「実績・お客様のエピソード」向けで、お知らせには合わない）。

## 2026-09-11 昼（三上様：通常投稿に固定投稿のようなものを混ぜない）

- 誘導文の付け足し停止・reservation_funnel 除外・quote_boost 停止・固定投稿下書きの分離（コミット済み）。本番DBでは 1236（よくなる様の引用投稿）と 915・1023〜1025（固定投稿の下書き）を取り下げ済み。
- デプロイ後に確認：9/12 6時の生成に reservation_funnel が無い・本文末尾に「ご相談は…から」が付いていない・水曜(9/16)に quote_boost が動かない。

## 2026-09-11 昼（三上様：承認が間に合わない先生・コメント即時）

- awaiting_slide（30分おき 7:00〜21:30 JST）・evening_approval_reminder（18:00）・comment_watch 15分おき（7:00〜21:45）・遅い承認は翌朝10時台（shared/publishTiming.ts）。コミット済み、今夜反映。
- ✅ Threads Webhook 登録済み（2026-09-11 11:00ごろ・三上様のChromeで実施）：Meta App「Threads Studio」(1250891946948510) → ユースケース Threads API → 「ThreadsでWebhook」を追加 → Moderate を Subscribe（callback https://threads-studio.com/api/threads/webhook・検証OK・fields: replies / delete v1.0）。ダッシュボードに Test ボタンは無い。最初の実イベントは `docker logs | grep ThreadsWebhook` で確認。comment_watch（15分）は保険として残す。
- デプロイ後に確認：`[AwaitingSlide]` が30分ごとに出る／18:00に `[EveningReminder] 送信` ／comment_watch が15分ごと（API 429 が出ないこと）。
- 承認待ちのまま公開ゼロの方への案内（朝のまとめ・18時）に「承認がないと投稿されない」と「自動にする（確認なし）」ボタン（d705277）。デプロイ後：該当者が出た朝に `★昨日は、承認待ちのまま` が本文に入っているか確認。

## 2026-09-11 昼（三上様「すべてその通りに」：代理店プラン）

- コミット済み（今夜反映）：APIアクセス表記の削除／公式LINE月間枠の監視（9:30 社内報告に残量、7割で運営へ警告）／代理店向け週次まとめ `agency_weekly_report`（月曜 9:05 JST・server/agencyReportJob.ts）／配下クライアントの制限・投稿消失を代理店へ即時連絡／ログイン前トップ（Landing.tsx）の料金欄を実態に合わせる／代理店クライアントの料金ページに「代理店契約に含まれています」。
- 通し確認（ローカルQA・QA_SAFE_MODE=1）：代理店テストID→クライアント発行→クライアントでログイン→LINE友だち追加の1本道→Threads連携 0/1 まで正常。クライアント管理メニューはクライアントに出ない。
- 資料：LP（threads-studio-lp）に代理店プランを追記して公開済み。カタログ（service-catalog）は services.json に代理店を足して build-all 済み（デプロイは週次同期タスク）。代理店紹介資料（ts-deck/ThreadsStudio_代理店プランのご案内.pptx/.pdf）はFAQに「白ラベルではない」「週次まとめ」を足して再生成済み（資料台帳・Notionの差し替えは未）。
- デプロイ後に確認：9/15（月）9:05 に `[AgencyReport] 代理店契約なし` が出る（契約が入るまで送信なし）／9:30 の社内報告に「公式LINEの月間枠」が出る。

## 宿題（規模が増える前に）
- [x] 朝6時の生成を並列化（2026-09-12 実装・ユーザー単位4並列・`AUTO_POST_CONCURRENCY`）。ローカルQAで通し確認済み（2人・9枠生成・失敗0・10分・エラーなし）。デプロイ後の確認：9/13 6時のログに `[AutoPost] 並列4で N人を処理（M分）` が出て、所要が75分→20分前後に短くなっていること。AIのレート制限エラー（429）が出ていないこと。

## 2026-09-12 昼（三上様指示3件）

- **公開前の確認の既定を「見送りしなければ予定時刻に公開」に**（migration `0085_soft_approval.sql`・全員 autoPublishIfNoResponse=1）。executor が予定時刻を過ぎた当日分の承認待ちを公開へ、前日以前の承認待ちは見送り。書き直し中は予定を1時間後ろへ。9/13 7:40 のお知らせ（publish_unless_declined_2026-09-13・確認ONの方にだけ本文）。戻すのは設定「OKした分だけ公開に戻す」（s=softappr&v=off）。
- **ご案内先URLの種類はドメインで判定**（shared/projectLinks.ts inferLinkTypeFromUrl／normalizeLinkType、parseProjectLinks で常時補正、LINE登録時に訂正を案内）。佐々木様（7269）の「Instagram＝広告LP」は本番データを公式HPに修正済み。
- デプロイ後に確認：
  - [ ] 9/13 の executor ログに `見送りなしのため公開へ` が出て、岩根様の当日分が予定時刻に公開される／前日以前の承認待ち（岩根様9件・香取様5件・比嘉様3件のうち9/12以前作成分）が「日をまたいだため見送り」になる
  - [ ] 9/13 7:40 のお知らせが確認ONの方に届く
  - [ ] 固定投稿のコメントに「ふだんの様子」が Instagram 以外で出ない

